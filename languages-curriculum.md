# Languages Curriculum（学習カリキュラム要求）

> **このファイルは最上流の要求仕様です。**
> 具体的な決定事項（ライブラリ選定・バージョン・API・しきい値など）は、選択言語の `<出力ディレクトリ>/master-spec.md` を参照してください。Learning MISRA の具体ルール本文は、同じ出力ディレクトリの `misra-spec.md` を参照してください。
>
> **本ファイルの位置付け**: 次の依存順で派生する生成パイプラインの**最上流ソース**として使用する。下流の仕様書・プロンプト・生成物はすべて本ファイルの記述を起点に派生させ、要求の変更は原則として本ファイルから行う（下流から逆流させない）。
>
> ```
> README.md + languages-curriculum.md
>     ↓ 初回のみ: パイプラインプロンプト 9 ファイルをブートストラップ
> languages-curriculum.md（本ファイル：上流要求）
>     ↓ prompts/pipeline/00-common-control.md          → target_language を 1 件ユーザに確認し状態チェック
>     ↓ prompts/pipeline/01-derive-output-spec.md      → <出力ディレクトリ>/output-spec.md（選択言語専用の出力仕様）
>     ↓ prompts/pipeline/02-derive-master-spec.md      → <出力ディレクトリ>/master-spec.md（選択言語専用の決定事項）
>     ↓ prompts/pipeline/03-derive-misra-spec.md       → <出力ディレクトリ>/misra-spec.md（選択言語専用の Learning MISRA 仕様）
>     ↓ prompts/pipeline/04-run-language-pipeline.md   → <出力ディレクトリ>/spec.md（選択言語専用の詳細仕様）
>                                                      → prompts/generation/<target_language>/generate-item.md
>     ↓ prompts/pipeline/05-generate-agent-context.md  → CLAUDE.md / AGENTS.md（リポジトリ共通の大規模生成用コンテキスト）
>     ↓ prompts/generation/<target_language>/generate-item.md      → 1 項目分の <出力ディレクトリ>/docs/ + <出力ディレクトリ>/projects/
>     ↓ ユーザ確認                                                 → 承認または修正指示
>     ↓ prompts/pipeline/91-verify.md                              → 項目単位の検証
>     ↓ prompts/pipeline/90-audit.md                               → 必要に応じて項目単位の監査
>     ↺ 未承認または不合格なら同じ項目を generate-item.md で再生成
>
> 項目生成フローとは独立して再実行する場合:
>     prompts/pipeline/91-verify.md                    → 生成済み成果物の必須検証
>     prompts/pipeline/90-audit.md                     → 必要に応じた未決定事項・矛盾・上流要求からの逸脱の監査
> ```
>
> **下流ドキュメントとの役割分担**:
> - **本ファイル**: 全言語候補に対する「何を学ぶか」「どの方針で作るか」（要求・スコープ・方針）
> - `<出力ディレクトリ>/output-spec.md`: 選択言語の「最終出力がどう見えるか」（ドキュメント構造・サンプルコード / CI/CD ワークフロー構成・章別必須コンテンツ・品質基準）
> - `<出力ディレクトリ>/master-spec.md`: 選択言語の「どのライブラリ／バージョン／API／しきい値を使うか」（具体的決定事項）
> - `<出力ディレクトリ>/misra-spec.md`: 選択言語へ適用する Learning MISRA のルール本文、適用例、検出方法、緩和条件
> - `<出力ディレクトリ>/spec.md`: 上記を統合した選択言語専用の生成仕様
>
> 共通の出力仕様、Learning MISRA 成果物、決定事項、言語別仕様書の共通雛形は作成しない。言語別仕様ファイルに非選択言語の決定やコード例を混在させない。
>
> 対象候補言語: **Rust**（`LearningRust2026/`）/ **Go**（`LearningGo2026/`）/ **C#**（`LearningCSharp2026/`）/ **Python**（`LearningPython2026/`）/ **Node.js**（`LearningNodejs2026/`）

## 目的

- プログラミング言語のハンズオンドキュメント作成
- ドキュメントに記載されたサンプルコード・プロジェクト・CI/CD ワークフローは、実行・動作確認可能
- ドキュメントの各項目は、完成コードの解説だけでなく、学習者が空の作業ディレクトリまたは直前の動作確認済み状態からコードを**自分で段階実装**し、各段階でビルド・テスト・実行結果を確認できるステップバイステップ形式にする
- ドキュメントの各項目には、学習者が手元で再現できる**実行可能なコードまたはワークフロー YAML**と、その生成物を含む**プロジェクトの構築手順またはワークフロー検証手順**（作成コマンド、ファイル配置、段階ごとの編集、ビルド、テスト、実行または CI 検証、期待出力または期待されるジョブ結果）を必ず含める
- 動作の流れ、データフロー、呼び出し順序、状態遷移、非同期処理、エラー経路などは Mermaid 図とコードの対応説明で必ず可視化する
- アルゴリズム章では言語構文の習得と同時にアルゴリズム自体の考え方を習得できるよう、概念、適用場面、図解、疑似コード、手計算トレース、実装コードとの対応を必須とする
- 学習者が**ビジネスアプリケーション**（CLI ツール、デスクトップ GUI アプリ、サーバ / Web API、データ処理パイプラインなど）を自力で作成し、既存サンプルへ機能を追加・置換できるようになること
- ドキュメントでは、選択言語用の `<出力ディレクトリ>/misra-spec.md` に定める Learning MISRA ガイドラインを適用し、以下を実現する
  - **セキュリティの向上**: 脆弱性を生みやすいコードパターンを禁止
  - **コード品質の均一化**: 開発者の経験に依存しない一貫したコード品質
  - **保守性の向上**: 誰が書いても同じ構造・スタイルになる予測可能なコード
  - **バグの予防**: 組み込み系開発の知見を活かした防御的プログラミング

## 対象言語の指定

学習ドキュメント / サンプルコード / CI/CD ワークフロー生成パイプラインは、1 回の実行で **1 言語**を `target_language` として指定し、その言語専用の `output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md`、学習ドキュメント、サンプルコード、CI/CD ワークフローを生成する。複数言語を生成する場合は、言語ごとに本パイプラインを独立して実行する。

| `target_language` | 出力ディレクトリ | 状態 | 生成時の扱い |
|---|---|---|---|
| `rust` | `LearningRust2026/` | 対応中 | 共通章を展開し、各章の「対応中の言語での例」から Rust の例のみを展開する |
| `go` | `LearningGo2026/` | 対応中 | 共通章を展開し、各章の「対応中の言語での例」から Go の例のみを展開する |
| `csharp` | `LearningCSharp2026/` | 将来予定 | C# 固有要求・言語別決定事項が揃うまで生成対象外 |
| `python` | `LearningPython2026/` | 要追加定義 | Python 固有章・テンプレート適用ルール・ライブラリ決定事項を追加してから生成する |
| `nodejs` | `LearningNodejs2026/` | 要追加定義 | Node.js 固有章・テンプレート適用ルール・ライブラリ決定事項を追加してから生成する |

各出力ディレクトリ（`<出力ディレクトリ>/`）は、次の構成を持つ。下流の言語別仕様および各プロンプトは本規約を**単一ソース**として参照する。

| パス | 用途 |
|---|---|
| `<出力ディレクトリ>/output-spec.md` | 選択言語専用の出力構造・章別必須コンテンツ・品質基準 |
| `<出力ディレクトリ>/master-spec.md` | 選択言語専用のライブラリ・バージョン・API・しきい値などの決定事項 |
| `<出力ディレクトリ>/misra-spec.md` | 選択言語専用の Learning MISRA ルールセット |
| `<出力ディレクトリ>/spec.md` | 言語別仕様（`prompts/pipeline/04-run-language-pipeline.md` で生成） |
| `<出力ディレクトリ>/docs/` | 学習ドキュメント（`prompts/generation/<target_language>/generate-item.md` で項目単位に生成） |
| `<出力ディレクトリ>/projects/` | サンプルコード / サンプルプロジェクト / CI/CD ワークフロー（`prompts/generation/<target_language>/generate-item.md` で対応 docs と対に生成） |

リポジトリ直下には、`prompts/pipeline/05-generate-agent-context.md` で生成する `CLAUDE.md` と `AGENTS.md` を置く。これらは言語別出力ディレクトリ配下には作成せず、特定言語の値や進捗を固定しない。ドキュメント名・章別ファイル名・projects 配下の命名規則は、実行中の `generate-item.md` が指定する `<出力ディレクトリ>/output-spec.md` を参照する。

本表は最終出力先の**単位**を示すのみで、`<出力ディレクトリ>/docs/` および `<出力ディレクトリ>/projects/` の**内部構造**（章別ファイル名規約、`assets/` ディレクトリ・画像配置、用語集 / FAQ のファイル化規約、サンプルプロジェクト / CI/CD ワークフロー単位のディレクトリ分割、テストディレクトリ配置など）は `<出力ディレクトリ>/output-spec.md` を**単一ソース**として確定する。各章ディレクトリのトップとなる索引ドキュメントは、GitHub 上で章トップとして表示しやすいよう `README.md` とし、`index.md` は使用しない。`prompts/generation/<target_language>/generate-item.md` は本表と選択言語用 `output-spec.md` の組み合わせで出力先を確定する。

### 言語別 `output-spec.md` の生成対象台帳

`<出力ディレクトリ>/output-spec.md` は、docs / projects の生成開始前に、生成する全項目を固定した**生成対象台帳**を持つ。台帳の各行には最低限「項目 ID / 種別 / 章 / 項目名 / 対応 docs パス / 対応 project または workflow パス / 前提項目 ID / 生成順 / 対象プラットフォーム / 検証方式（ネイティブ、クロスビルド、実機、仮想ディスプレイ、目視確認）/ 適用する DoD」を記録する。

- 台帳の項目 ID を `spec.md`、`generate-item.md`、docs、projects、`91-verify.md`、`90-audit.md` の共通キーとして使用する
- 「DP」「ビット操作」「並行データ構造」などのカテゴリ名や「等」「その他」を台帳の最終行として残さず、生成対象となる具体的なアルゴリズム・データ構造ごとに ID を付与して正確な総件数を確定する
- `Queue` のように複数カテゴリへ現れる名称は、「基本データ構造のキュー」「並行処理用キュー」のように目的と実装契約を区別し、別の項目 ID、前提、コード、テストを割り当てる
- 第 8 / 10 / 11 章のサンプルアプリケーションは 34 件すべてに一意なアプリ ID を付与し、第 11 章はアプリ種別とフレームワークの組み合わせごとに別行とする
- 34 件の各アプリについて、生成前に機能 ID、利用者の操作または入力、期待結果、主要データ、受入条件、必要なテスト種別を列挙した機能台帳を `output-spec.md` 内に持つ。`TBD`、代表機能だけの記載、「その他」などが残るアプリは生成可能扱いにしない
- 各実行可能項目に Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS 64-bit ARM64、Ubuntu 24.04 LTS ARM64 の適用可否と検証方法を持たせる。Raspberry Pi のクロスビルドと実機検証は別の検証欄にし、クロスビルド成功だけで実機検証済みにしない
- 実行可能項目は、特定プラットフォームだけを扱うこと自体が学習目標の項目を除き、正式ターゲット 4 環境すべてを原則必須とする。「適用不可」で黙って除外することを禁止し、技術的に対応できない場合は、理由、代替案、影響範囲をユーザに確認して `<出力ディレクトリ>/master-spec.md` に承認済み例外を記録する
- macOS arm64 は配分外の補助サポートとして CI ビルド可否を別欄に記録し、正式ターゲット 3 区分・4 実行環境の件数や検証済み件数へ混入させない
- `output-spec.md` は構造・件数・DoDを決めるファイルであり、ライブラリ版や API などの具体値を重複決定しない。具体値が必要な行には、本ファイル末尾の「`master-spec.md` で決定すべき項目一覧」にある対応項目名を参照キーとして記録する。参照キーが確定している状態は `TBD` とみなさないが、対応する決定が `master-spec.md` に存在しない状態では `spec.md` 生成へ進まない
- 台帳には章別・種別の期待件数と実際の行数を併記し、不一致または未確定行が 1 件でもある状態で `spec.md` と `generate-item.md` を生成しない
- 項目追加・削除・分割時は、上流の本ファイルから `output-spec.md` を更新し、影響する後続仕様とプロンプトを再派生する。下流成果物だけで台帳を書き換えない

1 言語生成時のルール:

- `target_language` は必須入力とし、未指定なら 1 言語生成パイプラインを開始しない
- `output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md` は選択言語の出力ディレクトリ内だけに生成し、非選択言語の固有節・決定・コード例を含めない
- 共通の出力仕様、Learning MISRA 成果物、決定事項、言語別仕様書の共通雛形は生成しない
- `対応中` 以外の言語が指定された場合は、足りない要求・決定事項を列挙して停止し、推測で生成しない
- 言語別仕様・docs・projects の出力先は上表のディレクトリ配下に限定し、別言語のディレクトリを同時に作成しない。プロンプト管理ファイルは `prompts/pipeline/` または `prompts/generation/<target_language>/`、言語非依存のエージェントコンテキストはリポジトリ直下の `CLAUDE.md` / `AGENTS.md` を例外の出力先とする

`対応中` 以外の言語を選択した場合の再開フロー:

本再開フローの実行は補助プロンプト `prompts/pipeline/80-enable-language.md` を**単一の実行手段**とする。同プロンプトは 1 回の実行で 1 言語を扱い、言語の対応中化に必要な最上流要求だけを本ファイルへ追加・確定する。言語別仕様ファイルは作成せず、対応中化の完了後に通常パイプラインで生成する。

1. `prompts/pipeline/80-enable-language.md` を実行する。冒頭で AI が対象 `target_language` を 1 件ユーザに確認する
2. AI は対象言語表で現在 `対応中` の全言語を機械的に抽出し、それらを起点として差分テーブルを作成し、不足項目（言語バージョン、パッケージマネージャ、ロガー、引数解析、DI、単体テスト、ドキュメントコメント形式、章別言語例など）を依存関係順に草案化する
3. AI は草案を本ファイルに `<!-- DRAFT[80-enable-language]: ... -->` マーカー付きで書き込み、ユーザに 1 件ずつ確認しながら修正を確定する
4. すべてのマーカーが解消されたことを確認したうえで、本ファイル「対象言語の指定」表の状態列を `対応中` へ更新する
5. §「1 言語生成パイプライン」の手順 1 から通常実行を開始する

## 想定作業（パイプライン）

本作業は、`target_language` を指定して実行する**1 言語生成パイプライン**とする。言語非依存の共通仕様成果物を先に作る段階は設けない。`prompts/pipeline/` の実行用プロンプトは共通利用してよい。上流の `README.md` と本ファイルは共通入力として参照し、言語別成果物は選択言語の出力ディレクトリだけへ書き込む。生成用プロンプトとリポジトリ直下の `CLAUDE.md` / `AGENTS.md` を除き、他言語の出力ディレクトリを読み書きしない。

### パイプラインプロンプトの初回ブートストラップ契約

初期状態で `prompts/pipeline/` が存在しない場合は、`README.md` と本ファイルだけを入力として、通常パイプラインより先に次の 9 ファイルを生成する。この処理はパイプラインを実行するためのプロンプトを用意する**初回ブートストラップ**であり、言語別仕様や最終学習成果物を生成する段階ではない。

| ファイル | 責務 |
|---|---|
| `prompts/pipeline/00-common-control.md` | `target_language` を 1 件だけ確認し、対象言語表の状態、出力先、前提ファイルを検証する |
| `prompts/pipeline/01-derive-output-spec.md` | 本ファイルから選択言語専用の `output-spec.md` と生成対象台帳を派生する |
| `prompts/pipeline/02-derive-master-spec.md` | 本ファイルと選択言語用 `output-spec.md` から、ユーザ確認済みの具体的決定だけを `master-spec.md` へ記録する |
| `prompts/pipeline/03-derive-misra-spec.md` | 上流要求と選択言語用仕様から、選択言語専用の Learning MISRA ルール本文を派生する |
| `prompts/pipeline/04-run-language-pipeline.md` | 選択言語用の 4 仕様から `spec.md` と `prompts/generation/<target_language>/generate-item.md` を派生する |
| `prompts/pipeline/05-generate-agent-context.md` | `README.md` と本ファイルの横断ルールだけから、リポジトリ直下の言語非依存な `CLAUDE.md` / `AGENTS.md` を生成する |
| `prompts/pipeline/80-enable-language.md` | 未対応言語の不足要求を 1 件ずつユーザ確認し、本ファイルの最上流要求だけを更新する |
| `prompts/pipeline/90-audit.md` | 項目単位で上流から下流までの静的整合、未決定、矛盾、件数を監査する |
| `prompts/pipeline/91-verify.md` | 項目単位でドキュメント手順をクリーン環境から再現し、コード・図・ワークフロー・プラットフォーム要件を動的検証する |

ブートストラップには次の制約を適用する。

- 生成対象は上表の 9 ファイルだけとし、`output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md`、`generate-item.md`、docs、projects、`CLAUDE.md`、`AGENTS.md` を同時生成しない
- 各プロンプトに、目的、前提入力、読み取り可能範囲、出力先、禁止事項、未決定事項を 1 件ずつ質問する手順、完了条件、次に実行するプロンプトを明記する
- パイプラインプロンプトは複数言語から再利用できる制御・派生手順に限定し、特定言語のライブラリ、バージョン、コード例、出力ディレクトリを固定しない。言語固有値は `target_language` 確定後に言語別出力へ派生する
- 共通の `output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md`、共通仕様雛形、共通 Learning MISRA 成果物を作成しない
- 既存の 9 ファイルがある場合は、上表との欠落・名称・責務・入出力契約を監査し、上流ルールに変更があるファイルだけを更新する。同じ入力で不要な差分を発生させない
- 9 ファイルのいずれかが欠落または不整合の状態では通常パイプラインを開始せず、ブートストラップを完了してから `00-common-control.md` へ進む

### 1 言語生成パイプライン

1 言語生成パイプラインで使用するプロンプトは、用途別に次の 2 層へ分けて保存する。パイプライン制御・仕様派生用と、最終成果物生成用を同じディレクトリに混在させない。

- パイプライン実行プロンプト: `prompts/pipeline/`（target_language 確認、言語別仕様群の派生、監査、検証）
- 学習ドキュメント / サンプルコード / CI/CD ワークフロー生成プロンプト: `prompts/generation/<target_language>/`（最終成果物生成）

1. `prompts/pipeline/00-common-control.md` で `target_language` を 1 件ユーザに確認し、`対応中` であることを状態チェックする。`将来予定` / `要追加定義` の場合は不足事項を列挙して停止し、`80-enable-language.md` を案内する
2. `prompts/pipeline/01-derive-output-spec.md` で、本ファイルから選択言語専用の `<出力ディレクトリ>/output-spec.md` を派生する
3. `prompts/pipeline/02-derive-master-spec.md` で、本ファイルと `<出力ディレクトリ>/output-spec.md` から `<出力ディレクトリ>/master-spec.md` を派生する。非選択言語の決定事項は記録しない
4. `prompts/pipeline/03-derive-misra-spec.md` で、本ファイル、`<出力ディレクトリ>/output-spec.md`、`<出力ディレクトリ>/master-spec.md` から `<出力ディレクトリ>/misra-spec.md` を派生する。検出方法は `master-spec.md` で確定したツールチェーン、Linter、Formatter、静的解析ツールを参照し、バージョンや採用ツールを重複決定しない。本ファイルから参照される Learning MISRA ルール ID がすべて定義されていることを検証する
5. `prompts/pipeline/04-run-language-pipeline.md` で、上記 4 ファイルから `<出力ディレクトリ>/spec.md` と `prompts/generation/<target_language>/generate-item.md` を直接派生する。共通雛形は介在させない
6. `prompts/pipeline/05-generate-agent-context.md` で、`README.md` と本ファイルの横断ルールから、長い生成作業のためのリポジトリ直下 `CLAUDE.md` と `AGENTS.md` を生成する。選択言語の `output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md` から言語固有値を転記しない
7. `prompts/generation/<target_language>/generate-item.md` を入力して、選択言語のハンズオンドキュメントとサンプルコード・プロジェクト・CI/CD ワークフローを 1 項目分だけ対で生成する
8. 各項目の生成直後に、ユーザが生成内容を確認し、承認または修正指示を行う。未承認の場合は次項目へ進まず、同じ項目を再生成する
9. 承認済み項目に対して `prompts/pipeline/91-verify.md` を項目単位で実行し、成果物が本ファイルと選択言語用の `output-spec.md`、`master-spec.md`、`misra-spec.md`、`spec.md` に整合するか検証する
10. 必要に応じて `prompts/pipeline/90-audit.md` を同じ項目単位で実行し、上流要求からの逸脱、未決定事項、矛盾を監査する
11. ユーザ承認、検証合格、必要な監査の未解決指摘なしを満たした項目だけを完了扱いにし、次項目へ進む

複数言語を生成する場合は、言語ごとに本セクションの手順 1 以降を独立して実行する。他言語の出力ディレクトリ内の仕様・docs・projects を読み書きしない。

## ターゲット学習者

- **初心者**: プログラミング言語を新しく学ぶ、または経験が浅い開発者
- **経験者**: プログラミング言語の基礎を理解している中級〜上級開発者

> 「**同じ記述方法**」は**コード**に対して適用する。すなわち初心者・経験者とも MISRA 適用レベルを同一にし、誰が書いても同じ構造・スタイルになる予測可能なコードを目指す（詳細は §「ハンズオン作成方針」）。
>
> 一方、**ドキュメント記述**は学習者の理解度差に配慮する。§「学習ドキュメント記述品質」に従って**初心者向けの十分な説明**（ステップバイステップ、つまずきの明示、専門用語の事前説明、FAQ、図解）を必ず含めたうえで、経験者向けには**「もう知っている部分を折りたためる」提示方法**（`<details>` タグ等）を採る。
>
> したがって**コードは単一基準（MISRA 統一）**、**ドキュメントは複数読者層への配慮（情報量は確保、表示形式は柔軟）**、という二層の役割分担を採る。

## 開発環境の前提

- エディタ: Visual Studio Code
- 開発環境: WSL2 (Debian) + Windows
- AI エージェント: Codex、Claude Code 等を活用した開発
- ターゲットプラットフォーム: Windows (70%) / Linux (10%) / Raspberry Pi 4 以降 (20%)
  - Windows: x86_64
  - Linux: Ubuntu 24.04 LTS x86_64。WSL2 (Debian) は開発環境であり、この 10% の正式ターゲット OS とは区別する
  - Raspberry Pi: Raspberry Pi 4 以降のいずれか 1 台、ARM64（aarch64）限定。Raspberry Pi OS 64-bit と Ubuntu 24.04 LTS 64-bit の両方を対象にし、1 台の実機で OS を切り替えて順番に検証する
- 配分の意味: 上記割合は、プラットフォーム固有の説明、コード分岐、コマンド、トラブルシューティング、ビルド・テスト・実行確認に対する重点度とする。言語・アルゴリズムなどの共通説明を行数比で分割するものではない
- Raspberry Pi の利用範囲: ARM64 Linux 上での汎用アプリケーション開発、ネイティブビルド、クロスビルド、実行、テスト、配布、CI/CD を対象とする。GPIO、I²C、SPI、UART、PWM などのハードウェア制御は対象外とする
- Raspberry Pi への接続: Windows 上の MobaXterm から SSH で接続する。教材の単体 GUI アプリケーションは X11 フォワーディングで目視確認するが、デスクトップ全体の遠隔操作は対象外とする
- Raspberry Pi の CI: GitHub Actions のセルフホステッドランナーによる実機ネイティブビルド・テストを基本とし、ARM64 クロスビルドも学習対象にする。GUI の自動テストは仮想ディスプレイを使い、目視確認は MobaXterm の X11 フォワーディングで行う
- macOS サポート: 上記配分外の補助サポートとして CI でビルドし、学習者が Mac 所有の場合のデバッグ手順も文書化する

## ハンズオン作成方針（第3章テンプレートの適用ルール）

本セクションは、**第4章〜第11章のハンズオンドキュメントおよびサンプルコードを生成する際**に、第3章「プログラミング言語の基本テンプレート」の構成要素をどの程度・どの粒度で適用するかを定める。下流の `<出力ディレクトリ>/spec.md` およびプロンプト群（`prompts/pipeline/`、`prompts/generation/`）は本ルールを保持・展開すること。

### 適用スコープ

- **起点**: 第4章「プログラミング言語の基本」
- **波及**: 第5章以降も原則として同じルールを適用（章固有の事情で逸脱する場合は当該章内にその理由を明記）
- **対象外**: 第1章「環境構築」／第2章「プロジェクトの作成方法」／第3章「プログラミング言語の基本テンプレート」自身、および第12章「CI/CD」（節サンプルを伴わない章）

### 対象外章の出力ガイド

「対象外」は「節サンプル」の対象外であり、章自体の出力物が空という意味ではない。各対象外章は次の「節サンプル代替生成物」を持ち、`<出力ディレクトリ>/output-spec.md` の章別必須コンテンツチェックリスト派生時の起点とする。

| 章 | 節サンプル代替生成物 |
|---|---|
| 第 1 章「環境構築」 | OS 別環境構築手順ドキュメントと `<出力ディレクトリ>/projects/ch01-environment/<project-id>/` 配下の環境設定プロジェクト（`.vscode/settings.json`、`.vscode/extensions.json`、`.vscode/launch.json`、`.vscode/tasks.json`、言語別ツールチェーン固定ファイル、Linter / Formatter 設定、デバッガ設定、動作確認用の最小コード、README）。コードよりも手順と設定ファイルが主体だが、生成物として projects 側にも保存し、docs/projects 双方向リンクを持つ。 |
| 第 2 章「プロジェクトの作成方法」 | 最小プロジェクトテンプレート群（基本／複数ソース／ライブラリ参照／ワークスペース／パッケージマネージャ経由インストール手順／配布対応リポジトリ構造）。各テンプレートはビルド・実行可能で、第 3 章「プログラミング言語の基本テンプレート」に含まれる戻り値型テンプレートを遵守する。初心者が同じ操作を再現できるよう、プロジェクト新規作成、ファイル配置、ビルド、テスト、実行、クリーン、失敗時の確認までを 1 ステップ 1 動作で記載する。 |
| 第 3 章「プログラミング言語の基本テンプレート」 | テンプレート実装例（戻り値型テンプレート遵守の最小サンプル、ログ／引数解析／DI／単体テスト／ドキュメントコメントの最小組み合わせサンプル）。第 4 章以降の節サンプルが参照する**正準テンプレート**を提供する。各テンプレートは、実際のコード、コードブロック単位の詳細説明、実行可能プロジェクト、実行結果、動作確認方法、動作の意味の説明を必ず含める。 |
| 第 12 章「CI/CD」 | GitHub Actions ワークフロー定義サンプル（章ごとワークフロー YAML、マトリクスビルド、依存関係キャッシュ、テスト連携、カバレッジ、アーティファクト管理、リリース自動化、セキュリティスキャン、Raspberry Pi セルフホステッドランナー、ARM64 クロスビルド、実機配備・実行、GUI 仮想ディスプレイ検証）。すべて実行可能 YAML 形式とし、`<出力ディレクトリ>/projects/ch12-cicd/<workflow-id>/` 配下へ README とともに保存し、docs/projects 双方向リンクを持つ。ランナー構築項目は YAML に加えて必要な設定・運用手順を同じ project に含める。 |

`<出力ディレクトリ>/output-spec.md` 派生時、対象外章のチェックリストは上表の生成物を起点に組み立てる。対象外章でも MISRA 適用（戻り値型テンプレート、ドキュメントコメント、ロガー経由出力等）は本文・サンプル中に出現する箇所では引き続き必須となる。

### 適用粒度

- **節サンプル**: 各節の末尾に置く「節を代表する完結したサンプルプログラム（**エントリーポイント（`main` など）から実行して出力・動作を確認できる、ビルド・実行可能なプログラム**）」を本ルールの主対象とする。節サンプルは `<出力ディレクトリ>/projects/` 配下の実プロジェクトと対応させ、ドキュメント側にプロジェクト構築手順を記載する。初心者がまず `main` 実行で動作を確認できることを最優先とし、単体テストは節サンプルの主体に据えず、`main` 実行で動作を確認したうえで補完する位置づけとする（単体テストを必須とする章は §「適用項目（必須 / 条件付き必須 / 推奨）」を参照）
- **本文中スニペット**: シンタックス紹介のための短いコード片はルールの対象外。ただし「節サンプルでテンプレート適用形を必ず再掲する」運用とし、学習者が節ごとに完成形を見られる体験を担保する
- **選択言語固有の例**: 各章の項目に付随する「**対応中の言語での例**」（選択言語の核固有概念、応用固有概念、システム統合固有機能など）も、節サンプルが存在する限り同等に適用する。非選択言語の例は生成対象に含めない

### 段階実装型ハンズオンの必須プロトコル

すべてのハンズオン項目は、完成コードを掲載して説明するだけの形式を禁止し、学習者が実際に手を動かして完成状態へ到達する**段階実装型**とする。開始状態は空の作業ディレクトリ、言語公式コマンドで作った最小プロジェクト、または本文中で直前に完成・検証した状態のいずれかを明示する。

各実装ステップは次の順序と内容を必ず持つ。

1. **このステップの目的**: 今から追加する機能・概念と、追加する理由を初心者向けに説明する
2. **開始時点の状態**: 使用する直前ステップ、存在するファイル、実行できる状態を明示する
3. **操作**: 作業ディレクトリ、対象 OS / シェル、実行するコマンド、作成・編集するファイルパス、挿入位置を 1 ステップ 1 動作で示す。依存追加は、その依存を参照するコードより先に行う
4. **入力するコード**: そのステップで変更する学習者管理下のソース・設定ファイルは、編集後の完全な内容をファイルパス付きで掲載する。差分は変更点の強調として併記してよいが、差分だけを入力手順の代替にしない。`...` や「同様に追加」では省略しない
5. **コードの説明**: 新しく追加した行・関数・型・設定値が何を担当し、直前ステップとどうつながるかをコードの実行順に説明する
6. **動作の図解**: 制御フロー、データフロー、呼び出し順序、状態遷移、非同期処理、エラー経路のいずれかがある場合は Mermaid 図を掲載し、図のノードと実際のファイル・関数・型を対応付ける
7. **動作確認**: Formatter、Lint、ビルド、テスト、実行のうち当該段階に必要なコマンドを示す
8. **期待結果とチェックポイント**: 期待出力、画面、生成ファイル、終了コード、テスト結果、ステップ終了時の主要ファイルツリーを示し、成功判定をチェックリスト化する
9. **この段階で起きやすい失敗**: 実際のエラーメッセージ例、原因、直し方を最低 1 件示す

1 ステップで複数機能を一括実装しない。各ステップ終了時は、未定義の関数・型・ファイルや未追加の依存を残さず、原則としてビルドまたは検証可能な状態に保つ。やむを得ず一時的にビルド不能になる場合は、同じ番号付きステップ内で復旧させ、途中状態で次のステップへ進ませない。後続ステップで初めて作るコードを先に参照しない。

ドキュメントの冒頭には完成までの実装ロードマップを置き、各ステップで増える機能と確認方法を表にする。末尾には再現に必要な学習者管理下の全テキストファイルを再掲するが、完成コードは段階実装手順の代替にしてはならない。

### ドキュメントと実行可能プロジェクトの対応関係

第 1 章〜第 3 章と第 12 章の節サンプル代替生成物、および第 4 章〜第 11 章の各節、各アルゴリズム、各実践アプリケーションは、学習者がハンズオンで再現できるように、次の成果物を 1 セットとして生成する。

1. `<出力ディレクトリ>/docs/` 配下の説明ドキュメント
2. `<出力ディレクトリ>/projects/` 配下のビルド・テスト・実行可能なプロジェクト、または実行可能ワークフロー定義
3. ドキュメントから対応プロジェクトまたはワークフローへの相対リンク
4. プロジェクト / ワークフロー README から対応ドキュメントへの相対リンク

各ドキュメント項目には、最低限次を含める。

- **作るもの**: この項目で完成させるプログラム、CLI、GUI、ライブラリ、またはワークフローの目的
- **完成までの実装ロードマップ**: 各段階で追加する機能、編集ファイル、動作確認方法を一覧化する
- **完成コードまたはワークフロー YAML の場所**: 対応する `<出力ディレクトリ>/projects/...` の相対パス
- **段階実装手順またはワークフロー構築・検証手順**: §「段階実装型ハンズオンの必須プロトコル」に従い、新規作成、依存追加、ファイル作成・編集、機能追加、ビルド、テスト、実行または CI 検証を番号付きで進める
- **実行可能なコードまたはワークフロー YAML**: ドキュメント本文に主要ファイルのコードまたは YAML を掲載し、長いコードや YAML は対応プロジェクトへのリンクを併記する。本文中スニペットだけで完結扱いにしない
- **このコードまたはワークフローで確認できること**: 実行・テスト・ログ・出力・エラー処理・状態変化・ジョブ結果を通じて、学習者が何を確認できるのかを箇条書きで明示する
- **コード / YAML の読み方**: 主要な関数、型、変数、分岐、エラー経路、ログ出力、テスト対象、ワークフローのトリガーやジョブが何を担当しているかを、コードまたは YAML の流れに沿って説明する
- **動作図とコードの対応**: 動作に関わる Mermaid 図を置き、図中の各要素がどのファイル・関数・型・ジョブに対応するかを表または箇条書きで説明する
- **まとめ: 動作する完全コード**: 各節・各項目の末尾に、対応プロジェクトまたはワークフローを再現するために必要な**学習者管理下のテキストファイル**の完全な内容を掲載する。リンクだけで代替せず、ファイルパス、ファイル内容、実行コマンドまたは検証手順、期待出力または期待されるジョブ結果、確認ポイントをまとめて再掲する。ビルド成果物、キャッシュ、外部依存の展開物、バイナリ、実行時一時ファイル、実トークン・秘密鍵・パスワードなどの秘密情報は掲載対象外とし、秘密値は明示的なプレースホルダと安全な設定手順で示す
- **期待される結果**: コマンド出力、生成ファイル、画面表示、ログ、終了コード、期待されるジョブ結果を記載する
- **確認方法**: 学習者が成功・失敗を判定できるチェックリスト、テストコマンド、ワークフロー検証手順、トラブルシューティングを記載する
- **プラットフォーム対応表**: Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS 64-bit ARM64、Ubuntu 24.04 LTS ARM64 ごとに、ビルド、テスト、実行、配布、GUI 表示の適用可否と確認方法を示す。共通手順と差分手順を分け、Raspberry Pi はクロスビルド結果と実機結果を別々に記録する
- **差分学習**: 既存コードを変更する手順では、変更前後の要点と変更理由を説明する

`<出力ディレクトリ>/output-spec.md` は、この対応関係を章別必須コンテンツチェックリスト、ドキュメント内部レイアウト、プロジェクト / ワークフロー内部レイアウト、生成物トレーサビリティ表として具体化する。`prompts/generation/<target_language>/generate-item.md` は、同じ項目 ID / プロジェクト ID を使って docs と projects を対で生成し、ドキュメントだけ、またはコード / ワークフローだけが生成される状態を禁止する。

### 大規模生成用エージェントコンテキスト

学習ドキュメントとプロジェクトコードの生成は大規模になるため、`generate-item.md` を実行する前に、`prompts/pipeline/05-generate-agent-context.md` で次のファイルをリポジトリ直下へ生成する。

- `CLAUDE.md`: Claude Code 向けの生成コンテキスト、品質基準、章別分割方針、再開手順
- `AGENTS.md`: Codex / 汎用コードエージェント向けの生成コンテキスト、編集ルール、検証ルール、再開手順

両ファイルには、少なくとも次を含める。

- ユーザへの質問・確認・最終報告を日本語で行うルール
- docs / projects 生成前に `languages-curriculum.md` の §「戻り値型テンプレートの適用基準」、§「学習ドキュメント記述品質」、§「生成時の品質保証（薄まり防止の強制ルール）」を毎回確認するルール
- docs / projects の命名規則と内部構造は、実行中の生成プロンプトが指定する `<出力ディレクトリ>/output-spec.md` を単一ソースとして確認し、各章ディレクトリの索引ドキュメントは `README.md` にするルール
- `target_language`、出力ディレクトリ、言語別決定事項は生成時に対象言語の `<出力ディレクトリ>/master-spec.md` と `<出力ディレクトリ>/spec.md` から確認し、`CLAUDE.md` / `AGENTS.md` 自体へ固定しないルール
- `CLAUDE.md` / `AGENTS.md` の生成を target_language に対して冪等にし、別言語のパイプラインを実行しても言語名、言語別パス、ライブラリ、バージョン、進捗だけを理由とする差分を発生させないルール
- 既存の `CLAUDE.md` / `AGENTS.md` がある場合は、全言語共通の上流ルールに変更があるときだけ更新し、選択言語固有の情報は `generate-item.md` から実行時に受け取るルール
- 1 回の生成で扱う章・節・プロジェクトを小さく区切る方針
- 全ハンズオンを段階実装型とし、各ステップの目的、操作、コード、説明、図解、検証、期待結果、失敗例を省略しないルール
- 各項目の生成直後にユーザ確認を行い、項目単位で `91-verify.md` と必要な `90-audit.md` を実行してから次へ進む方針
- 各節末尾の「まとめ: 動作する完全コード」を省略しないルール
- docs/projects 相互リンク、項目 ID / プロジェクト ID、トレーサビリティ表の維持ルール
- アルゴリズム章の概念説明、Mermaid 図、疑似コード、コード対応表、変数遷移表、トレース出力、C 言語比較を省略しないルール
- 全 34 サンプルアプリケーションを個別生成し、機能一覧、機能とコードの対応、機能単位の段階実装、拡張演習を省略しないルール
- Windows 70% / Ubuntu 24.04 LTS x86_64 10% / Raspberry Pi 4 以降 ARM64 20% の対象配分と、Raspberry Pi OS 64-bit・Ubuntu 24.04 LTS 64-bit の実機検証、ARM64 クロスビルド、MobaXterm SSH / X11 フォワーディング、GUI 仮想ディスプレイ検証を省略しないルール
- 中断・再開時に最初に確認する言語別仕様、生成対象台帳、各項目の DoD 自己チェック、docs/projects の実在、検証コマンド
- `<出力ディレクトリ>/master-spec.md` で確定したライブラリ、バージョン、API、OS 要件を推測で上書きしないルール

### 戻り値型テンプレートの適用基準

第 4 章から第 11 章までの節サンプルは、第 3 章「プログラミング言語の基本テンプレート」に含まれる「戻り値型テンプレート（成功/失敗・不在/存在）」を規範として次の基準に従う。

- 許可される戻り値型は「成功/失敗を表す型」「不在/存在を表す型」「副作用なし純関数の単純戻り」に限定する
- 通常失敗は戻り値経路で明示的に伝播する
- `panic!` / `unwrap` / 例外送出による異常系流用は禁止し、不変条件違反のみに限る
- 不在値と失敗値は意味を分離し、言語ごとの標準イディオムに合わせて型で表現する
- 言語別表現は第 3 章「対応中の言語での例」の `Option<T>` / `Result<T, E>` / `(T, error)` / `(T, bool)` などに準拠する
- 対象言語で標準的な表現が複数ある場合は、`<出力ディレクトリ>/master-spec.md` で採用基準を確定してから節サンプルへ反映する

### 適用項目（必須 / 条件付き必須 / 推奨）

| 区分 | 項目 | 適用内容 |
|---|---|---|
| **必須** | 戻り値型テンプレート（成功/失敗・不在/存在） | 本セクション「戻り値型テンプレートの適用基準」と第 3 章「プログラミング言語の基本テンプレート」内の戻り値型テンプレート項目に一括準拠する |
| **必須** | ドキュメントコメント | 公開関数・型・モジュールに付与（Rust: `///` / Go: パッケージ・公開シンボル向け `//` コメント。その他言語は `<出力ディレクトリ>/master-spec.md` で形式を決定） |
| **条件付き必須** | ログライブラリ | **節サンプルにコンソール出力が存在する場合は必ず採用**。生の `println!` / `fmt.Println` ではなくロガー経由で出力する |
| **推奨** | 引数解析ライブラリ | 節のトピックに自然に適合する場合に採用（例: CLI 入出力を扱う節） |
| **推奨** | DI ライブラリ | 節のトピックに自然に適合する場合に採用（例: 外部依存を持つ節） |
| **章により必須 / 推奨** | 単体テストライブラリ | いずれの章も節サンプル・プロジェクトは `main` 実行を主体とし、単体テストはそれを補完する。**第 6 章アルゴリズム・第 8 章実践プロジェクト・第 10 章 CLI・第 11 章 GUI は単体テスト必須**。第 3 章テンプレート（「単体テストテンプレート」自体を除く）・第 4 / 5 / 7 / 9 章の節サンプルは推奨（テスト可能な関数を含む節は積極採用） |

### 運用上の補足

- 節サンプル・実践プロジェクトは `main`（各言語のエントリーポイント）から実行して期待出力を確認できる構成を主体とし、初心者がまず `main` 実行で動作を確認できることを最優先する。単体テストは `main` 実行を確認したうえで追加し、**第 6 章アルゴリズム・第 8 章実践プロジェクト・第 10 章 CLI・第 11 章 GUI では必須**、第 3 章テンプレート（「単体テストテンプレート」自体を除く）・第 4 / 5 / 7 / 9 章では推奨とする
- 「推奨」項目は節のトピックと自然に適合する場合に採用する。無理な詰め込みは避け、学習者の認知負荷を最優先で考慮する
- 「条件付き必須」が主題に該当しない場合は、そのチェック項目だけを理由付きで非適用にする。「必須」が噛み合わないことを理由に、生成対象台帳に登録された節サンプル、アルゴリズム、アプリ、ワークフロー自体を省略してはならない。適用方法に矛盾がある場合は生成前に停止し、`<出力ディレクトリ>/output-spec.md` の項目定義をユーザ確認のうえ修正する
- 下流の `<出力ディレクトリ>/spec.md` および `prompts/generation/<target_language>/` 生成においては、本ルールを選択言語の各章ごとの「テンプレート適用チェックリスト」へ機械的に展開すること
- 本ルールの適用対象章（第 4〜第 11 章）の末尾には「**【テンプレート適用】**: 「ハンズオン作成方針」参照」の 1 行を必ず置き、本セクションへの逆引きを保つ（第 1〜第 3 章および第 12 章は対象外）
- `<出力ディレクトリ>/output-spec.md` の「章別必須コンテンツチェックリスト」は、本セクションの「適用項目」表全行を**各節の節サンプルチェック列に踏襲して展開**する。具体的には、第 4 章〜第 11 章の各節ごとに「戻り値型テンプレート（必須）」「ドキュメントコメント（必須）」「ログライブラリ（条件付き必須：節がコンソール出力を含むなら）」「引数解析 / DI（推奨：節のトピックに自然に適合するなら）」「単体テスト（第 6 / 8 / 10 / 11 章は必須、第 4 / 5 / 7 / 9 章は推奨）」をチェック項目として並べる。章固有の出力要件は上記の横断ルールと**重ねて**チェックする
- `<出力ディレクトリ>/output-spec.md` のチェックリストは、各項目について「対応プロジェクト ID」「完成コードの相対パス」「段階実装ロードマップ」「各ステップの必須 9 要素」「Mermaid 図とコード対応」「ビルド / テスト / 実行コマンド」「期待出力」「理解確認」「拡張課題」「docs/projects 相互リンク」を検証列として持つこと
- 対象外章（第 1, 2, 3, 12 章）では「節サンプル」概念は適用しないが、節サンプル代替生成物に出現するコードには MISRA 適用箇所が引き続き適用される。`<出力ディレクトリ>/output-spec.md` のチェックリストは「§対象外章の出力ガイド」を起点に組み立てる
- `prompts/pipeline/91-verify.md` は本セクションの「適用項目」表全行（戻り値型テンプレート、ドキュメントコメント、ログライブラリ、引数解析、DI、単体テスト等）の遵守を検証項目に含め、欠落・違反があれば当該項目の `generate-item.md` 再実行を案内する（次セクション §「学習ドキュメント記述品質」§「運用上の補足」の検証連動と対をなす）

## 学習ドキュメント記述品質

本セクションは、最終生成物のうち**学習者が読む文章**が満たすべき記述品質を定める。プログラミング初心者が予備知識を推測せずに読み進め、説明と手元のコードを対応させられるよう、下記 7 項目を**最低基準**として遵守すること。前節「ハンズオン作成方針」が**コード品質と実装手順**を定めるのに対し、本セクションは**理解のための説明品質**を定め、両者は両立して適用する。

### 適用範囲

- **主たる適用先**: `<出力ディレクトリ>/docs/` 配下の全章本文（章別 .md ファイル）、および `<出力ディレクトリ>/projects/` 配下の `README.md`・手順書・説明ドキュメント
- **限定的な適用先**: `<出力ディレクトリ>/projects/` 配下のコードコメントには「専門用語の事前説明（項目 6）」と「初心者がつまずきやすい点のコメント（項目 2）」のみ適用する
- **対象章**: 全 12 章共通で適用する（§「ハンズオン作成方針」の対象外章である第 1, 2, 3, 12 章にも本セクションは適用される）
- **ハンズオン手順**: 第 1 章〜第 3 章と第 12 章の節サンプル代替生成物、および第 4 章〜第 11 章の各項目、各アルゴリズム、各実践アプリケーションでは、本文説明だけでなく、設定ファイル、実行可能コード、または実行可能ワークフロー YAML を学習者が段階実装する手順を必須とする
- 下流の `<出力ディレクトリ>/output-spec.md` は本セクションを「章別必須コンテンツチェックリスト」の記述品質列に展開し、`prompts/generation/<target_language>/generate-item.md` は生成時に本基準を遵守する

### 1. 初心者にやさしい文章口調

- 「です・ます」調を基本とし、断定的・命令的な言い回しを避ける
- 「〜してください」「〜という方法もあります」「〜できます」など、学習者の選択を尊重する表現を使う
- 1 段落 1 トピックを目安にし、複数の概念を畳みかけない。長文は箇条書きまたは小見出しで分解する
- 新しい概念は「何のためのものか」→「身近な例または既知概念との関係」→「最小例」→「実際のアプリでの使い方」の順で説明し、定義だけで済ませない
- コードブロックの前に「このコードで何をするか」、後に「各行がどう動くか」と「実行すると何が起きるか」を記載する

### 2. 初心者がつまずきやすい点の明示

- 各節または各手順の末尾に「**ここでよくある間違い**」「**ハマりやすいポイント**」見出しを設け、想定される失敗例とその原因・対処を 1 件以上記載する
- 検証時に実際に取得したエラーメッセージ（コードブロックでのテキスト引用、または該当箇所のスクリーンショット）を含め、それを見たときに学習者が何をすべきかを明示する。架空の出力を実測結果として記載せず、ユーザ名、絶対パス、ホスト名、IP アドレス、トークンなどは意味を損なわない範囲で伏せる
- 「環境差異（OS / シェル / バージョン）が原因の失敗」「順序を誤りやすい操作」は特に手厚く記述する

### 3. ステップバイステップの手順

- 連続した操作を 1 段落に詰め込まず、番号付きリストで 1 ステップ 1 動作に分解する
- 各ステップで「**何を入力するか**」「**何が起きるか**（期待される画面・出力・ファイル状態）」「**次に何をするか**」を明示する
- 環境構築・コマンド実行・ビルド・テスト・配布など迷いやすい操作は特に細かく分解し、各ステップに期待出力を併記する
- コードを扱う項目は §「段階実装型ハンズオンの必須プロトコル」の 9 要素を各ステップに適用し、完成コードの一括コピーだけで手順を終えない
- 各ステップの最後に「ここまでで理解したこと」を 1〜3 項目で要約し、次のステップで何を追加するかを接続する

### 4. トラブルシューティング / よくある質問（FAQ）

- 章末またはセクション末に「**困ったとき**」または「**よくある質問（FAQ）**」セクションを置く
- Q&A 形式で、想定される疑問・遭遇しやすい問題と回答を記載する
- 検索ヒント（エラー文字列のどの部分を Web 検索すれば答えに辿り着くか、公式ドキュメントの該当ページ等）も併記する

### 5. 視覚的な補助（図解）

- 動作の流れ、データフロー、関数・サービス間の呼び出し、状態遷移、非同期処理、エラー経路の説明には **Mermaid 図を必須**とする。単純な静的構造だけの場合は ASCII 図または埋め込み画像でもよい
- 処理の分岐には `flowchart`、呼び出し順序には `sequenceDiagram`、状態変化には `stateDiagram-v2`、データ構造や型関係には `classDiagram` または適切な Mermaid 形式を使い分ける
- 図を掲載するだけで終えず、図中の各ノード・矢印・状態が、どのファイル、関数、型、コード範囲に対応するかを直後に説明する
- 段階実装で機能が増えた場合は図も更新し、完成後の図だけを先に見せて途中の理解を省略しない
- ファイル構成・ディレクトリ構造はツリー形式（`tree` コマンド風の出力）で示す
- Mermaid の構文がレンダリング可能であることを検証し、図と本文・コードの用語を一致させる
- 画像ファイルの配置規約は `<出力ディレクトリ>/output-spec.md` で確定する
- Mermaid の検証ツール、バージョン、実行コマンドは `<出力ディレクトリ>/master-spec.md` で決定し、`91-verify.md` で全変更図を検証する

### 6. 専門用語の事前説明

- 専門用語が初出する箇所では、その用語の意味を 1〜2 文で説明してから使う
- 章末または巻末に**用語集**を置き、別章からも再参照可能にする
- 「カタカナ用語」「英語アクロニム（CLI / FFI / DI 等）」「他言語からの借用語」は特に丁寧に説明する。略語は初出時にフルスペル → 略形の順で示す（例: `FFI`（Foreign Function Interface、外部関数インターフェース））

### 7. 学習目標・前提知識・理解確認

- 各項目の冒頭に「この項目でできるようになること」を、観測可能な行動として 2〜5 件示す
- 必要な前提知識と参照すべき既習項目を明示し、未説明の概念を既知として扱わない
- 各主要セクションの末尾に、コードを実行・変更して答えを確認できる短い理解確認を置く
- 項目末尾に、完成コードの単純な再入力では解けない小さな変更課題と、確認方法、期待される振る舞いを示す
- 解答例を載せる場合は `<details>` で折り畳み、先に学習者が考えられる構成にする

### 運用上の補足

- 本基準は記述の**最低基準**であり、章のトピックに応じて記述量を増やしてよい。初心者向け説明を要約して減らしてはならず、「項目 2（つまずき）」「項目 4（FAQ）」「項目 6（専門用語）」「項目 7（理解確認）」は最低 1 件を必ず残す
- `prompts/pipeline/91-verify.md` は本基準の遵守を検証項目に含め、欠落があれば当該項目の `generate-item.md` 再実行を案内する
- 経験者にとって冗長と感じる補足は `<details>` タグ等で折り畳んでよいが、実装手順、コード説明、期待結果、エラー対処、図とコードの対応は折り畳みを理由に省略しない

## 生成時の品質保証（薄まり防止の強制ルール）

本セクションは、`<出力ディレクトリ>/docs/` と `<出力ディレクトリ>/projects/` を**大規模生成する際に、要求が記述されているにもかかわらず生成物が薄くなる**（項目の抜け、要約への逃避、未完成コード / YAML、件数不足）ことを防ぐための**強制ルール**を定める。前 2 セクション（§「ハンズオン作成方針」＝コード品質、§「学習ドキュメント記述品質」＝記述品質）が「**何を含めるか**」を定めるのに対し、本セクションは「**それを薄めずに必ず生成しきること**」を保証する手続きを定める。

下流の `<出力ディレクトリ>/output-spec.md`、`<出力ディレクトリ>/master-spec.md`、`<出力ディレクトリ>/misra-spec.md`、`<出力ディレクトリ>/spec.md`、`prompts/generation/<target_language>/generate-item.md`、`CLAUDE.md` / `AGENTS.md` は本ルールを保持・展開し、`prompts/pipeline/91-verify.md`（動的検証）と `prompts/pipeline/90-audit.md`（静的整合）は本ルールを**ブロッキング検証**として適用する。

### 1. 完成見本（ゴールドスタンダード）の整備と複製

- 生成物の種類ごとに、**要求を 100% 満たした「完成見本」を 1 つだけ先に用意**し、ユーザ承認と `91-verify.md` 合格後に、以降の同種項目へ完成見本の**構成・深さ・必須要素の並び**を適用する。分量はトピックの難易度に合わせて増減できるが、完成見本にある必須要素を削らない。未承認または検証不合格の見本を複製しない。
- 完成見本を用意する種類（最低限）:
  - 第 1 章の環境設定プロジェクト（1 項目分）
  - 第 2 章のハンズオン手順（1 項目分）
  - 第 3 章の正準テンプレート（1 テンプレート分）
  - 第 4・5・7・8・9 章の節サンプル（各章 1 節分）
  - 第 6 章のアルゴリズムドキュメント（1 アルゴリズム分。**概念説明・Mermaid 図・疑似コード・コード対応・段階実装・変数遷移表・トレース出力・C 言語比較を含む完成形**）
  - 第 8 章の Web API / データ処理実践アプリ（1 アプリ分）
  - 第 10 章の実践 CLI アプリ（1 アプリ分）
  - 第 11 章の実践 GUI アプリ（1 アプリ × 1 フレームワーク分）
  - 第 12 章の CI/CD ワークフロー（1 ワークフロー分）
- 完成見本の骨格と選択言語の具体例は、選択言語用の `<出力ディレクトリ>/output-spec.md` と `<出力ディレクトリ>/spec.md` に定義する。共通雛形は作成しない。`generate-item.md` と `CLAUDE.md` / `AGENTS.md` は、この完成見本を「**複製元テンプレート**」として明示参照する。

### 2. 1 項目ずつ完成させる生成プロトコル（バッチ要約の禁止）

- 生成は**1 項目を完成させてから次へ進む**。ここでの 1 項目とは「1 ハンズオン手順 / 1 テンプレート / 1 節サンプル / 1 アルゴリズム / 1 実践アプリ（GUI は 1 アプリ × 1 フレームワーク）/ 1 CI/CD ワークフロー」を指す。
- 1 項目の「完成」とは、次がすべて揃った状態を指す。
  1. `<出力ディレクトリ>/docs/` の説明ドキュメント
  2. `<出力ディレクトリ>/projects/` のビルド・テスト・実行可能なプロジェクト、または実行可能ワークフロー定義（再現に必要な学習者管理下の**全テキストファイル**）
  3. 完成までの実装ロードマップと、§「段階実装型ハンズオンの必須プロトコル」を満たす実装手順
  4. 動作を説明する Mermaid 図と、図の要素からコードへの対応説明
  5. ドキュメント末尾の「まとめ: 動作する完全コード」（再現に必要な学習者管理下の全テキストファイルの完全内容。第 12 章ではワークフロー YAML を含む）
  6. docs ↔ projects 相互リンク
  7. 理解確認と変更・拡張課題
  8. 本セクション §4 の完成基準（DoD）自己チェック結果
- 各生成単位の直後にユーザ確認を行い、承認が得られるまで次の項目へ進まない。
- 承認済み項目は、項目 ID / プロジェクト ID を指定して `prompts/pipeline/91-verify.md` を実行し、必要に応じて同じ単位で `prompts/pipeline/90-audit.md` を実行する。
- ユーザ未承認、`91-verify.md` 不合格、または `90-audit.md` の未解決指摘がある項目は、同じ `generate-item.md` で docs/projects を対にしたまま再生成する。
- 1 項目を完成させたら、「**完了した項目 / 次に生成する項目 / 残件数**」を明記してから次の項目へ進む。**複数項目をまとめて生成し要約で済ませることを禁止**する。
- コンテキストが長くなる場合は項目境界で区切る。再開時は `<出力ディレクトリ>/output-spec.md` の生成対象台帳を基準に、対応 docs/projects の実在、各項目の DoD 自己チェック、直近の検証結果を照合して完了・未完了を再構成する。仕様ファイルを進捗表として書き換えず、`CLAUDE.md` / `AGENTS.md` にも言語別・セッション別の進捗状態を固定しない。ユーザ承認の記録を確認できない項目は、承認済みと推測せずユーザに確認する。

### 3. 手抜き・省略表現の禁止

- ドキュメント本文・コード・YAML・「まとめ: 動作する完全コード」において、次の**省略・先送り表現を禁止**する。
  - 「以下省略」「（中略）」「（略）」「同様に」「他も同様」「以下同様」「（実装は省略）」「紙面の都合で…」など、内容の省略を示す表現
  - 完成コードの一部を `// ...`・`/* 略 */`・`# 以下同様` 等で省くこと、ファイル内容の途中打ち切り
  - トピックが「TODO スタブ」「未実装の表現方法」自体でない限り、`todo!()` / `unimplemented!` / 空実装 / プレースホルダ本体でコードを未完成のまま残すこと。また、第 12 章のワークフローを空 YAML や説明文だけで代替すること
- 件数が定義された生成対象（§6）は「等」「など」で省略せず、**定義された全要素を必ず列挙・生成**する。
- 再現に必要な学習者管理下のテキストファイルは「まとめ: 動作する完全コード」で**全文を必ず再掲**する。リンク参照だけで本文掲載を省かない。ビルド成果物、キャッシュ、外部依存の展開物、バイナリ、一時ファイル、秘密情報は再掲せず、除外理由と再生成・取得方法を示す。

### 4. 完成基準（DoD）と生成中の自己チェック

- 生成物の種類ごとに**完成基準（Definition of Done）チェックリスト**を持ち、生成器は**各項目を出力した直後に DoD 各項目の合否（✓/✗ と根拠の相対パス・該当箇所）を自己申告**する。✗ が 1 つでも残る間は次の項目へ進まない。
- 種類別 DoD の最低項目:
  - **`main` 実行とテストの扱い（全種類共通）**: 節サンプル・テンプレート・実践プロジェクトは `main`（エントリーポイント）から実行して期待出力を確認できることを主体とし、これを必須とする。単体テストは第 6 章アルゴリズム・第 8 章実践プロジェクト・第 10 章 CLI・第 11 章 GUI で必須、第 3 章テンプレート（「単体テストテンプレート」自体を除く）・第 4 / 5 / 7 / 9 章で推奨とする
  - **共通**: 学習目標 / 前提知識 / 作るもの / 完成までの実装ロードマップ / 完成コードまたはワークフロー YAML の場所 / 各段階の目的・開始状態・操作・入力コード・コード説明・Mermaid 図・検証・期待結果・失敗例 / 主要コードまたは主要 YAML / このコードまたはワークフローで確認できること / 図とコードの対応 / まとめ: 動作する完全コード / ビルド・テスト・Lint・Formatter・実行コマンドまたは CI 検証手順 / 期待出力または期待されるジョブ結果 / 確認方法 / **対象プラットフォーム対応表とプラットフォーム別検証証跡** / トラブルシューティング / 理解確認 / 変更・拡張課題 / docs ↔ projects 相互リンク / 戻り値型テンプレート遵守 / ドキュメントコメント
  - **第 1 章**: OS 別前提 / VSCode 拡張一覧 / `.vscode/settings.json`・`.vscode/extensions.json`・`.vscode/launch.json`・`.vscode/tasks.json` の完全内容 / 言語別ツールチェーン固定ファイル / Linter・Formatter・デバッガ設定 / 動作確認用の最小コード / 環境確認コマンド / ビルド・テスト・Lint・Formatter・デバッグ起動の確認手順 / Ubuntu 24.04 LTS x86_64 の構築 / Raspberry Pi OS 64-bit・Ubuntu 24.04 LTS 64-bit の構築 / MobaXterm SSH・X11 フォワーディング確認 / 期待出力 / よくある失敗と対処 / ch01 環境設定プロジェクトへのリンク
  - **第 2 章**: 事前確認 / 新規作成コマンド / 生成されるファイル構成 / 編集ファイルの完全内容 / 各ファイルの役割 / ビルド・テスト・Lint・Formatter・実行・クリーン / 各コマンドの期待出力 / 成功判定ポイント / よくある失敗と対処 / ch02 プロジェクトへのリンク
  - **第 3 章**: 目的・適用場面・非適用場面 / 初心者向け概念説明 / 完全なテンプレートコード / コードブロック単位の詳細説明 / このコードで確認できること / ch03 テンプレートプロジェクト / 動作確認手順（正常系・失敗系・不在値・入力エラー・ログ・テスト失敗時の見方）/ 実行結果の意味 / テンプレート ID と相対リンク
  - **第 6 章（アルゴリズム）**: アルゴリズム名と解決する問題 / 適用・非適用場面 / 入出力・前提・実用例 / **アルゴリズム自体の考え方** / Mermaid による処理フローまたは状態遷移 / 手計算トレース / 疑似コード / 疑似コード・図・選択言語コードの対応表 / 主要変数とデータ構造の意味 / **固定サンプル入力に対する変数遷移表** / 反復ごとの条件判定と次状態 / **実行ログまたはトレース出力** / 段階実装 / 計算量（Big-O ＋具体例）/ 境界条件 / 選択言語の実行可能コード / `main` 実行 / **単体テスト（必須）** / **`c-comparison/` の実行可能な C 比較実装、比較説明、正式ターゲット 4 環境での警告なしビルド・テスト** / 理解確認 / パラメータまたは入力を変える発展課題
  - **第 8 / 10 / 11 章（全サンプルアプリ共通）**: 要件 / 対象利用者と利用シナリオ / 全機能一覧 / 機能間の関係を示す Mermaid 図 / 機能から画面・入出力・データ・ファイル・型・関数・テストへのトレーサビリティ表 / 機能単位の段階実装 / 正常系・異常系・境界値テスト / 完成後の機能追加・置換・設計変更を行う拡張演習
  - **第 8 章（Web API / データ処理の各アプリ）**: API またはジョブの入出力仕様 / データモデル / 処理フロー / 永続化またはキュー処理 / 入力検証 / エラー処理 / ログ・メトリクス / 冪等性・再試行 / 実行可能プロジェクト・構築・テスト・運用確認手順
  - **第 10 章（CLI 各アプリ）**: コマンド例 / 設定ファイル例 / 標準出力・標準エラー / 終了コード / ログ / エラー処理 / テストケース / 期待出力 / トラブルシューティング / 実行可能プロジェクト・構築・テスト・配布手順
  - **第 11 章（GUI 各アプリ × 各 FW）**: 画面仕様 / 画面遷移 / 状態管理 / IPC・コマンド呼び出し / 入力検証 / エラー表示 / アクセシビリティ / E2E テスト / スクリーンショット確認 / OS 別ビルド手順 / Raspberry Pi 上の仮想ディスプレイ自動テスト / MobaXterm X11 フォワーディングによる単体アプリ目視確認 / ARM64 成果物 / 期待される画面状態 / 実行可能プロジェクト・構築・テスト・配布手順
  - **第 12 章（CI/CD）**: ワークフロー目的 / トリガー / ジョブ構成 / マトリクス軸 / 依存関係キャッシュ / Lint・Formatter・テスト・カバレッジ連携 / アーティファクト保存 / リリース自動化 / セキュリティスキャン / シークレット管理 / Raspberry Pi 実機セルフホステッドランナー / 1 台での OS 切替運用 / ARM64 クロスビルド / クロス成果物の実機配備・実行 / GUI 仮想ディスプレイ / 実行可能 YAML・検証手順・期待されるジョブ結果
- DoD は `<出力ディレクトリ>/output-spec.md` の「章別必須コンテンツチェックリスト」と一致させ、`CLAUDE.md` / `AGENTS.md` にも共通ルールとして転記する。

### 5. ブロッキング検証ゲート

- `prompts/pipeline/91-verify.md` は**ブロッキングゲート**として動作する。1 項目でも DoD・構造・件数のいずれかを満たさない場合、生成は**不合格**とし、当該項目だけを `generate-item.md` で**合格するまで再生成**する。
- ハンズオン項目の検証は完成済み project のビルド・テストだけで合格にしない。検証用の空ディレクトリを用意し、学習ドキュメントに記載された開始状態、コマンド、ファイル内容を手順順に再現して、各ステップの Formatter / Lint / ビルド / テスト / 実行と期待結果を確認する
- 検証中にドキュメント外の補完、未記載コマンド、完成 project からのファイルコピーが必要になった場合は不合格とする。OS や外部サービスのため実行不能な確認は、`<出力ディレクトリ>/master-spec.md` で決定した代替検証方法と根拠がある場合に限り認める
- Raspberry Pi 対応項目は、ARM64 クロスビルドと Raspberry Pi OS 64-bit / Ubuntu 24.04 LTS 64-bit の実機検証を別々に実行・記録する。1 台の OS 切替により一方が未実行の間は「保留」とし、クロスビルド、エミュレーション、他方の OS の結果で代替合格にしない
- 最終ステップの学習者管理下ファイルを対応 project と比較し、意図した差分がないことを確認する。ビルド成果物、キャッシュ、実行時一時ファイルなど比較対象外は `<出力ディレクトリ>/output-spec.md` で明示する
- ✗ が 1 つでも残る状態でパイプラインを「完了」と報告しない。検証結果は「**合格項目数 / 不合格項目数 / 不合格項目の一覧（種類・ID・欠落 DoD・再生成先プロンプト）**」の形で報告する。
- 不合格が記述品質・コード品質ルール（§「ハンズオン作成方針」§「学習ドキュメント記述品質」）に起因する場合も同様にブロックする。

### 6. 件数・構造の機械的ゲート

- 次の**件数**を満たさない場合は不合格とする（「ほぼ揃っている」を認めない）。
  - 第 1 章 環境設定プロジェクト: 第 1 章に列挙された環境設定項目の**全件**
  - 第 3 章 正準テンプレートプロジェクト: **最低 9 種**（最小アプリ / 成功・失敗 / 不在・存在 / ログ / 引数解析 / DI / 単体テスト / ドキュメントコメント / 統合業務アプリ最小）。言語固有テンプレートを追加する場合は、追加後の正確な件数を `<出力ディレクトリ>/output-spec.md` の台帳へ確定する
  - 第 6 章 アルゴリズム: §「章立て」第 6 章に列挙された**各アルゴリズム／データ構造を独立した項目**として 1 つも欠かさず生成。スラッシュ区切りや括弧内の各名称をまとめて 1 ドキュメントにしない
  - 第 8 章 Web API / データ処理 実践プロジェクト: **4 種**
  - 第 10 章 実践 CLI アプリ: **12 種**
  - 第 11 章 実践 GUI アプリ: **簡易タスク管理 1 種 + 業務アプリ 5 種 = 6 種 × 3 フレームワーク ＝ 18 組み合わせ**
  - 第 8 / 10 / 11 章 サンプルアプリケーション合計: **4 + 12 + 18 = 34 件**
  - 第 12 章 Raspberry Pi CI/CD 追加項目: **4 件**（セルフホステッドランナー構築・OS 切替 / 2 OS ネイティブ CI・結果集約 / ARM64 クロスビルド・実機配備 / GUI 仮想ディスプレイ・X11 目視確認）
- 次の**構造要素の実在**を満たさない場合は当該項目を不合格とする（プロンプト本文に「含める」と書くだけでなく、生成物に実体があることを確認する）。
  - 各節・各項目の末尾に「まとめ: 動作する完全コード」が存在し、再現に必要な学習者管理下の**全テキストファイルが全文掲載**されている。生成物、バイナリ、キャッシュ、一時ファイル、秘密情報は除外され、再生成・取得・安全な設定方法が説明されている
  - 各項目に段階実装ロードマップがあり、各ステップに 9 要素（目的 / 開始状態 / 操作 / 編集後の完全なコード / コード説明 / 必要な Mermaid 図 / 動作確認 / 期待結果・チェックポイント / 失敗例）が実在する
  - 動作を扱う各項目に有効な Mermaid 図と、図中要素からコードへの対応説明が実在する
  - 各アルゴリズムに「**アルゴリズム自体の説明**」「**Mermaid 図**」「**疑似コードと選択言語コードの対応表**」「**変数遷移表**」「**実行ログ／トレース出力**」「**`c-comparison/` の実行可能な C 比較実装と正式ターゲット 4 環境の検証証跡**」が実在する
  - 34 件の各サンプルアプリに全機能一覧、機能トレーサビリティ表、機能単位の段階実装、テスト、拡張演習が実在する
  - 第 12 章の CI/CD 項目に、実行可能な GitHub Actions YAML、検証手順、期待されるジョブ結果、docs ↔ projects の双方向リンクが実在する
  - 各実行可能項目に対象プラットフォーム対応表があり、Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS 64-bit ARM64、Ubuntu 24.04 LTS ARM64 の確認状態と証跡が区別されている。Raspberry Pi ではクロスビルドと実機検証の両方が区別して記録されている
  - 第 12 章に、Raspberry Pi のセルフホステッドランナー構築・安全な運用、1 台で 2 OS を切り替える手順、ARM64 クロスビルド、成果物の実機検証、GUI 仮想ディスプレイ検証が実在する
  - 各 docs 項目に対応する `<出力ディレクトリ>/projects/` の実プロジェクトが実在し、相互リンクが両方向に張られている
- 件数・構造ゲートは `<出力ディレクトリ>/output-spec.md` のチェックリストへ展開し、`prompts/pipeline/91-verify.md`（動的検証）と `prompts/pipeline/90-audit.md`（静的整合）の双方で確認する。

## 章立て（12章構成）

本章立ては全 `target_language` 共通の骨格として使う。各章は言語非依存の項目を主軸とし、言語固有の例は子項目として「**対応中の言語での例**」の形で記載する。生成時は §「対象言語の指定」の対象言語表で `対応中` の言語に対応する共通章と選択言語固有節のみを展開する。

### 1. 環境構築

本章は、環境構築手順だけで終えず、VSCode と言語別ツールチェーン設定を再利用できる環境設定プロジェクトを `<出力ディレクトリ>/projects/ch01-environment/<project-id>/` に生成する。第 2 章・第 3 章と同様に、章用ディレクトリ配下へ項目ごとの `project-id` ディレクトリを置き、docs/projects 双方向リンクを持たせる。

各環境設定プロジェクトには最低限次を含める。

- `.vscode/settings.json`、`.vscode/extensions.json`、`.vscode/launch.json`、`.vscode/tasks.json`
- 言語別ツールチェーン固定ファイル（例: `rust-toolchain.toml`、`go.mod` の `toolchain`、`.tool-versions` など。採用は `<出力ディレクトリ>/master-spec.md` に従う）
- Linter / Formatter / デバッガの設定ファイルと、設定値の役割説明
- 環境確認・ビルド・テスト・Lint・Formatter・デバッグ起動を確認するための最小コードとコマンド
- OS・アーキテクチャ差異（Windows x86_64、WSL2 Debian、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS 64-bit ARM64、Ubuntu 24.04 LTS ARM64、macOS arm64）と、失敗時の確認ポイント

- Visual Studio Code
- WSL2 (Debian) + Windows
- Ubuntu 24.04 LTS x86_64 のネイティブ環境
- Raspberry Pi 4 以降の ARM64 実機環境
  - Raspberry Pi OS 64-bit と Ubuntu 24.04 LTS 64-bit の導入、OS・CPU アーキテクチャ・メモリ・ディスク空き容量・時刻同期の確認
  - Windows 上の MobaXterm からの SSH 鍵接続、ホスト鍵確認、ファイル転送、切断・再接続、SSH 接続失敗時の切り分け
  - X11 フォワーディングの有効化、単体 GUI アプリケーションの起動確認、`DISPLAY`・X 認証・必要パッケージ・ファイアウォールのトラブルシューティング。デスクトップ全体の遠隔操作手順は生成しない
  - 選択言語のツールチェーン、Linter、Formatter、デバッガを実機へ導入し、最小コードをネイティブにビルド・テスト・実行する
- Raspberry Pi の GPIO、I²C、SPI、UART、PWM などのハードウェア制御環境は生成対象に含めない
- 第 6 章の比較実装をビルド・実行するための C ツールチェーン（コンパイラ、C 言語標準、警告、デバッガ、サニタイザまたは同等検査）を正式ターゲット 4 環境へ導入する
- Codex CLI、Claude Code 設定
- 言語別ツールチェーン
  - **対応中の言語での例**:
    - Rust: `rust-toolchain.toml`
    - Go: `go.mod` + toolchain
  - 選択言語の決定事項は `<出力ディレクトリ>/master-spec.md` 参照
- バージョンマネージャ（複数バージョン併用、プロジェクト別バージョン固定）
  - **対応中の言語での例**:
    - Rust: `rustup`（`rust-toolchain.toml` で固定）
    - Go: `asdf`（`.tool-versions` で多言語一元・プロジェクト別固定）/ `g` / `gvm` / `goenv`（採用は `<出力ディレクトリ>/master-spec.md` で決定。`asdf` 非対応の Windows ネイティブ環境は `g` 等で補完）
- Linter / Formatter 設定
- デバッガ設定（VSCode デバッガ拡張、`launch.json` 設定、ブレークポイント、変数監視、ステップ実行）
  - **対応中の言語での例**:
    - Rust: `CodeLLDB` または `rust-analyzer` 経由のデバッグ
    - Go: `Delve`（`dlv`）+ Go 拡張機能
- macOS デバッグ手順

### 2. プロジェクトの作成方法

本章は、初心者が最初に手を動かす章として、すべての項目をステップバイステップのハンズオン形式で生成する。説明だけで終えず、各項目ごとに実際のプロジェクトを作成し、ビルド・テスト・実行・クリーンまで確認する。

各項目には最低限次を含める。

- 事前確認（必要なツール、バージョン、作業ディレクトリ、シェル、OS 差異）
- 新規プロジェクト作成コマンドと、実行後に作られるファイル / ディレクトリ構造
- 学習者が作成・編集するファイルの完全な内容
- 各ファイルの役割、設定値、依存関係、エントリポイントの説明
- ビルド、実行、単体テスト、Lint、Formatter、クリーンのコマンド
- Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi ARM64 でのネイティブビルド手順と、実行ファイル・ライブラリ・依存物が OS / アーキテクチャをまたいでそのまま再利用できない理由
- ARM64 クロスビルドの基礎（ターゲット指定、リンカ / sysroot / C 依存の有無、成果物の識別）。具体的な CI と実機配備は第 12 章で段階実装する
- 各コマンドの期待出力と、成功したと判断できる確認ポイント
- よくある失敗（パス違い、依存解決失敗、ロックファイル差分、ツール未導入、OS 差異）と対処
- 対応する `<出力ディレクトリ>/projects/ch02-projects/<project-id>/` 配下の実行可能プロジェクトへのリンク

- 基本的なプロジェクト
- 複数のソースファイルから構成されるプロジェクト
- ライブラリのプロジェクトとそれを参照するプロジェクト
- ビルド / 実行 / テスト / クリーンの基本操作
- ワークスペース / モノレポ構成（複数パッケージ、共有ライブラリ、サンプルプロジェクトの配置）
- パッケージマネージャと依存管理エコシステム（**ライブラリ依存**の管理：ビルド時に取り込む外部ライブラリの宣言・解決・ロック）
  - **対応中の言語での例**:
    - Rust: Cargo（`Cargo.toml`、`Cargo.lock`、ワークスペース）
    - Go: Go modules（`go.mod`、`go.sum`、`replace` ディレクティブ）
- 依存関係の追加 / 更新 / 固定 / 脆弱性確認
- パッケージマネージャ経由のツール / バイナリのインストール（**実行可能 CLI ツール**を `$PATH` に取り込んで使う側。前項のライブラリ依存とは別概念）
  - レジストリ経由インストール（公式レジストリから安定版を取得）
  - Git URL 直結インストール（リポジトリから直接インストール。レジストリ未公開のツールや内部公開ツールの取り込み）
  - バージョン指定インストール（タグ／ブランチ／コミット／semver で固定し、再現性を確保）
  - ローカルパスインストール（開発中ツールを `$PATH` に置いて反復検証）
  - インストール済みツールの一覧表示とアンインストール（ツールライフサイクル管理）
  - **対応中の言語での例**:
    - Rust: `cargo install <crate>`（レジストリ）／ `cargo install --git <URL>` ／ `cargo install --git <URL> --tag <tag>` ／ `cargo install --git <URL> --branch <branch>` ／ `cargo install --git <URL> --rev <commit>` ／ `cargo install --version <semver>` ／ `cargo install --path <path>` ／ `cargo install --list` ／ `cargo uninstall <crate>`
    - Go: `go install <pkg>@<version>` ／ `go install <URL>@<tag>` ／ `go install <URL>@<commit>` ／ `go install <pkg>@latest` ／ `go install ./cmd/<tool>`（ローカル相対パス）／ `$GOPATH/bin`（`$(go env GOPATH)/bin`）配下のバイナリを一覧・手動削除
- プロジェクト設定ファイルの読み方（ビルド設定、依存設定、開発ツール設定）

### 3. プログラミング言語の基本テンプレート

> 本章の目的: メソッド / 関数の**戻り値型を共通テンプレート化**し、初心者でも誤らない MISRA 準拠・ビジネスアプリで安全に通用するコード基盤を確立する。第 4 章以降の節サンプルはすべて本章のテンプレートに従って書かれる。
> 節サンプルへの具体的な適用粒度は §「ハンズオン作成方針」の「戻り値型テンプレートの適用基準」を参照する。

本章は、以降すべてのコードの基準となるため、単なる方針説明ではなく、正準テンプレートとして再利用できる実行可能プロジェクト群を生成する。各テンプレート項目には最低限次を含める。

- テンプレートの目的、適用する場面、適用しない場面
- 初心者向けの概念説明（成功 / 失敗、不在 / 存在、副作用、ログ、DI、単体テスト、ドキュメントコメント）
- テンプレートコードの完全版（主要ファイルを本文に掲載し、再現に必要な学習者管理下の全テキストファイルは `<出力ディレクトリ>/projects/ch03-templates/<template-id>/` に配置。ビルド成果物、キャッシュ、外部依存の展開物、バイナリ、一時ファイル、秘密情報は含めない）
- コードブロック単位の詳細説明（入力、出力、戻り値、エラー経路、ログ、依存関係、テスト対象を説明する）
- プロジェクト構築手順（新規作成、依存追加、ファイル配置、ビルド、テスト、実行）
- 動作確認手順（正常系、失敗系、不在値、入力エラー、ログ出力、テスト失敗時の見方）
- 実行結果の説明（出力、ログ、終了コード、テスト結果が何を意味するか）
- 第 4 章以降の節サンプルが参照するためのテンプレート ID と相対リンク

正準テンプレートプロジェクトは、次の 9 種を必須としてすべて生成する。選択言語の理解に不可欠な言語固有テンプレートは、ユーザ確認後に追加してよい。

1. 最小アプリケーションテンプレート
2. 成功 / 失敗を戻り値で表すテンプレート
3. 不在 / 存在を戻り値で表すテンプレート
4. ログ出力テンプレート
5. 引数解析テンプレート
6. DI テンプレート
7. 単体テストテンプレート
8. ドキュメントコメントテンプレート
9. 上記を統合した業務アプリ最小テンプレート

- 戻り値型テンプレート（成功/失敗・不在/存在）
  - 戻り値型の制限方針（MISRA 適用）: 例外送出による異常系伝播・`null` 返却・`bool` 単独での成否表現・成功時のみ意味を持つ複数戻り値などを禁止し、許可される戻り値型を「成功/失敗を表す型」「不在/存在を表す型」「その単純戻り（副作用なし純関数）」に限定する
  - エラー処理方針（MISRA 適用）: 通常失敗は戻り値経路で明示的に伝播し、`panic!` / `unwrap` / 例外送出による異常系の流用を禁止する。パニック／例外は不変条件違反（=プログラムの前提が壊れた場合）に限る
  - 不在値と失敗値の言語別表現（不在=意味的に値が存在しない / 失敗=処理が完遂しなかった、を**型で分離**する原則）
    - **対応中の言語での例**:
      - Rust: 不在 `Option<T>`（`Some` / `None`）／失敗 `Result<T, E>`（`Ok` / `Err`）。`?` 演算子と `From`/`Into` でエラー伝播を統一
      - Go: 失敗 `(T, error)` / `error`（`errors.Is` / `errors.As` / `fmt.Errorf("...: %w", err)` で連鎖）。不在は `(T, bool)`（map lookup・type assertion など標準イディオムが存在する場合）または `(T, error)` で表現し、節ごとの採用基準は `<出力ディレクトリ>/master-spec.md` で確定
- ログライブラリ
- 引数解析ライブラリ
- DI ライブラリ
- 単体テストライブラリ
- ドキュメントコメント
- 入力検証、境界値チェック、リソース解放、キャンセル伝播

### 4. プログラミング言語の基本

- 各型の string format の組み合わせ
- 変数・定数・スコープ
- コメント、ドキュメントコメント、属性 / アノテーション相当のメタ情報
- 演算子、型変換、数値変換、文字列変換
- 制御構造
- プリミティブ型・文字列型・コレクション
- 値 / 参照 / 所有 / コピー / ムーブ相当のデータ受け渡しモデル
- 列挙型表現と分岐（列挙値の定義、値ベース／パターンベースの分岐、定数群の連番付与）
  - **対応中の言語での例**:
    - Rust: `enum` + `match`（網羅性チェック付きパターンマッチ）
    - Go: 定数群 + `iota`（定数の連番付与）、`switch` による分岐
- モジュール・パッケージ
- クロージャ / 関数リテラル
- ポインタ・参照
- 標準 I/O・ファイル I/O・時刻・日付・環境変数
- ファイル内/外グローバル変数・関数（アトミック含む）
- エラー、パニック、例外相当の異常系表現
  - 通常失敗と不変条件違反の区別（通常失敗は戻り値経路で伝播、不変条件違反のみパニック／例外で表現）
  - エラー伝播（戻り値経路でのエラー受け渡し、層境界でのエラー型変換、コンテキストラッピング、アンラッピング、エラー連鎖）
  - **対応中の言語での例**:
    - Rust: `?` 演算子による伝播、`From` / `Into` トレイトによる層境界での型変換、`Result<T, E>` 連鎖、`Box<dyn Error>` / `dyn Error::source()` でのチェーン辿り
    - Go: `fmt.Errorf("...: %w", err)` でのラップ、`errors.Is` / `errors.As` でのアンラップ判定、`errors.Unwrap` での連鎖辿り
- データ型と振る舞い（旧「クラス」）／可視性とカプセル化／再利用と拡張（旧「継承」の代替）／ポリモーフィズム／プロパティパターン
- 選択言語の核となる固有概念（メタプログラミング、メモリ管理モデル、並行プリミティブ、抽象化機構など、言語の中核を成す機能）
  - **対応中の言語での例**:
    - Rust: `macro_rules!`（宣言的マクロ）／所有権・借用・ライフタイム／スマートポインタ／Trait 詳細／From/Into 等
    - Go: `defer`／`goroutine` と `channel`／`select`／interface と embedding／`context.Context` 基礎／`panic`/`recover`（理解目的のみ）
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 5. プログラミング言語の応用

- ジェネリクス詳細
- ラムダ式・関数合成・関数型コレクション操作（旧「C# みたいな LINQ」）
- イテレーション / 写像 / フィルタ / 畳み込みによるデータ変換
- 非同期プログラミング（タスク、キャンセル、タイムアウト、バックプレッシャ）
- 並行プリミティブ（スレッド / タスク / チャネル / ロック / アトミック）
- 並行安全性の表現（値が複数スレッド／並行コンテキストで安全に共有・送受信できることをどう表現するか）
  - **対応中の言語での例**:
    - Rust: `Send` / `Sync` トレイトによる型レベルマーキング
    - Go: goroutine-safe な値設計（mutex / channel / atomic 経由のアクセス、データ競合検出）
- エラー処理の応用（第 4 章「エラー伝播」の発展）
  - 独自エラー型階層の設計（複数バリアントの定義、ライブラリ境界での公開エラー型と内部エラー型の分離）
  - エラー集約（複数エラーを単一値で保持・報告、部分失敗の表現）
  - エラー分類とリトライ戦略（リトライ可能 / 不可・致命的 / 警告の分類、backoff、最大試行回数）
  - **対応中の言語での例**:
    - Rust: `thiserror` でのエラー型派生、`anyhow` / `eyre` での境界外エラー集約、カスタム enum エラー＋`From` 連鎖、retry / backoff クレート（採用は `<出力ディレクトリ>/master-spec.md` で決定）
    - Go: 独自 `error` 型の `Error()` / `Unwrap()` 実装、`errors.Join` での複数エラー保持、sentinel error と type assertion による分類、リトライ可能性判定インターフェース
- 選択言語の応用的な固有概念（高度なメタプログラミング、動的ディスパッチ／静的ディスパッチの選択、低レベル機能、リフレクション、リソース埋め込みなど）
  - **対応中の言語での例**:
    - Rust: 手続きマクロ（function-like / derive / attribute）／Trait object vs ジェネリクス／`unsafe` 基礎／`Pin` と自己参照
    - Go: `reflect`（制限下）／`embed` パッケージ
- 設計パターン（ビルダー、型駆動、イテレータ自作）
- 性能計測と最適化の基礎（ベンチマーク、プロファイリング、アロケーション削減）
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 6. アルゴリズム

> 再帰禁止推奨の方針に従い、再帰が自然なアルゴリズムは**再帰版・非再帰版の両方を実装**。実装不可能な場合はルール逸脱を明記。

各アルゴリズムまたはデータ構造は、カテゴリ単位でまとめず、**1 種類につき 1 つの独立した学習ドキュメントと実行可能プロジェクト**を生成する。「探索」だけを説明して二分探索・DFS・BFS を同一項目で済ませることや、代表例だけを生成して残りを省略することを禁止する。カテゴリ名や総称しか定まっていない項目は、`<出力ディレクトリ>/output-spec.md` で具体的な生成対象を列挙し、ユーザ確認後に生成を開始する。

各項目は、初心者が選択言語とアルゴリズム自体を同時に理解できるよう、次の順序で構成する。

- 問題設定、入力、出力、前提条件、実用例を初心者向けに説明する
- コードを提示する前に、アルゴリズムが「何を観察し」「どの規則で次の状態を選び」「いつ終了するか」を、専門用語を説明しながら平易な文章で解説する
- 適している場面、適していない場面、別のアルゴリズムを選ぶ判断基準を説明する
- アルゴリズムの考え方を、Mermaid のフローチャートまたは状態遷移図、手計算の逐次トレース、言語非依存の疑似コードで説明する
- **概念・図・疑似コード・実装コード対応表**を設け、「アルゴリズム上の役割 / Mermaid のノード / 疑似コード行 / 選択言語のファイル・関数・型・コード範囲」を対応付ける
- 実行可能コードの主要変数、データ構造、ループカウンタ、条件分岐の意味を説明する
- サンプル入力を 1 つ以上固定し、各ステップで変数に入る値、コレクションの中身、探索位置、比較対象、戻り値候補がどう変化するかを表形式で示す
- ループまたは再帰の各反復で「今見ている値」「次に更新する値」「条件判定の結果」「次の状態」を説明し、コード行・疑似コード・図解を対応付ける
- 実行ログまたはトレース出力を用意し、ドキュメントの変数遷移表と照合できるようにする
- 計算量（時間計算量 / 空間計算量）を Big-O と具体例の両方で説明する
- 境界条件、空入力、重複値、オーバーフロー、巨大入力、並行実行時の注意点を説明する
- 選択言語の実行可能コードを `<出力ディレクトリ>/projects/ch06-algorithms/<algorithm-id>/` に生成し、ドキュメント側では「最小の入出力 → 中核データ構造 → アルゴリズム本体 → トレース → エラー・境界条件 → テスト」の順に段階実装する
- C 言語の比較実装を同じドキュメントに掲載し、同じ project の `c-comparison/` 配下へビルド・実行・テスト可能な形で配置する。比較対象は、メモリ管理、ポインタ / 参照、配列境界、エラー表現、整数オーバーフロー、標準ライブラリ利用、所有権 / ライフタイム相当概念、テスト容易性とする
- C 言語実装は選択言語の実装と同じ固定入力・期待出力・境界値で動作させ、正式ターゲット 4 環境でコンパイル・テストする。C 側で未定義動作を避ける境界チェックとエラー処理を明示し、警告をエラーとして扱うビルドと、利用可能な環境でのサニタイザまたは同等検査を行う
- 項目末尾に「入力を変えた手計算」「一部を実装し直す」「別方式と比較する」の理解確認を置き、解答では図・疑似コード・実装の対応を再確認できるようにする

- 基本データ構造（Stack / Queue / Priority Queue / Linked List / Hash Table / Tree / Balanced Tree / Ring Buffer。各名称を起点にし、Linked List の単方向・双方向、Hash Table の衝突解決、Tree / Balanced Tree の具体方式など、カテゴリのままでは実装契約が一意にならないものは `<出力ディレクトリ>/output-spec.md` で具体方式へ分解して独立項目にする）
- 探索（二分探索 / DFS / BFS。各名称を独立項目にする）
- ソート（Quicksort / Mergesort / Heapsort。各名称を独立項目にし、custom comparator は各方式の発展演習として扱う）
- 実用アルゴリズム・パターン（LRU / Trie / Bloom Filter / State Machine。各名称を独立項目にする）
- 並行処理パターン（Mailbox（メッセージ受信箱）/ Concurrent Queue / Rate Limiter / Circuit Breaker / Retry。各名称を独立項目にし、基本データ構造の Queue とは別 ID・別の並行安全性契約で扱う）
- 文字列（KMP / Levenshtein。各名称を独立項目にする）
- データ操作パターン（反復 / 写像 / フィルタ / 畳み込み。各名称を独立項目にする）
- 動的計画法（DP。具体的な問題を `<出力ディレクトリ>/output-spec.md` で列挙し、問題ごとに独立項目にする）
- グラフ（ダイクストラ / A* / トポロジカルソート / 最小全域木 / Union-Find。各名称を独立項目にする）
- ビット操作（具体的な演算・問題を `<出力ディレクトリ>/output-spec.md` で列挙し、独立項目にする）
- 数値アルゴリズム（GCD / 素数判定・列挙 / modular exponentiation。各名称を独立項目にする）
- 組み込み観点（FNV / xxHash / MurmurHash / CRC / チェックサム / 外部ソート / 並行データ構造。各名称を独立項目にする。CRC、チェックサム、並行データ構造の具体方式は `<出力ディレクトリ>/output-spec.md` で列挙する）
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 7. 実用処理（基盤 I/O）

- ファイルシステム詳細
- 標準入出力詳細（バッファリング、リダイレクト、ttyの判定、終端文字の扱い）
- 環境変数の高度な操作（読み書き、子プロセスへの伝播、`.env` ファイルの取り扱い、機密情報の扱い）
- ネットワーク I/O（TCP / UDP / Unix Domain Socket）
- バイナリ I/O（バイト列、エンディアン、固定長 / 可変長レコード、ストリーム処理）
- ファイル監視（変更検知、デバウンス、プラットフォーム差異）
- Windows、Ubuntu 24.04 LTS x86_64、Raspberry Pi の ARM64 Linux で生じるパス区切り、大文字小文字、改行、実行権限、シンボリックリンク、ファイル監視、Unix Domain Socket、シグナルの差異
- 非同期 I/O プリミティブ（イベントループ、`epoll` / `kqueue` / `IOCP` 抽象、ノンブロッキング I/O）
  - **対応中の言語での例**:
    - Rust: `tokio` / `async-std` 等のランタイム（採用は `<出力ディレクトリ>/master-spec.md` で決定）
    - Go: `net` パッケージのデフォルト非同期動作、`io.Reader`/`io.Writer` インターフェース
- パイプ通信（プロセス間の標準入出力リダイレクト、名前付きパイプ、`stdin`/`stdout`/`stderr` の連結）
- プロセス管理・シグナル
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 8. 実用処理（データ処理）

- シリアライゼーション詳細（テキスト形式: JSON / TOML / YAML、バイナリ形式: MsgPack 等のスキーマレス + 言語固有のバイナリ形式）
  - **対応中の言語での例**:
    - Rust: テキスト系 (`serde` + 各ライブラリ) / バイナリ系 (`bincode`、`postcard` 等)
    - Go: テキスト系 (`encoding/json`、`gopkg.in/yaml.v3` 等) / バイナリ系 (`encoding/gob` 等)
- 設定管理の統合（TOML + 環境変数 + 引数の優先順位）
- データ検証（スキーマ検証、型検証、入力正規化、エラーレポート）
- HTTP クライアント・サーバ詳細
- データベース接続
- マイグレーション、トランザクション、コネクションプール
- 暗号プリミティブ
- 圧縮・解凍
- 正規表現
- UUID 生成
- テンプレートエンジン
- ログ詳細（ローテーション・構造化・出力振り分け）
- キャッシュ実装
- キュー / ストリーム処理（バッチ処理、再試行、冪等性）
- Raspberry Pi をヘッドレスな ARM64 Linux サーバとして使う実行・運用確認（SSH 配備、環境変数、ログ、シグナルによる正常終了、再起動後の復旧）。サービス管理方法と権限は `<出力ディレクトリ>/master-spec.md` で決定する
- Web API / データ処理 実践プロジェクト:
  1. CRUD Web API（ルーティング、入力検証、DB 接続、マイグレーション、トランザクション、構造化ログ）
  2. 認証・認可つき業務 API（認証方式、セッション / トークン管理、権限チェック、監査ログ。方式は `<出力ディレクトリ>/master-spec.md` で決定）
  3. バッチ ETL（CSV / JSON / DB 入出力、再実行可能性、冪等性、エラーレポート）
  4. ストリーム / キュー処理（ジョブキュー、再試行、バックオフ、デッドレター相当、メトリクス）
- 上記 4 種をすべて個別の学習ドキュメントと実行可能プロジェクトとして生成する。各ドキュメントはアプリの全機能を機能 ID 付きで列挙し、「利用者の操作または入力 / 期待結果 / データ / 担当ファイル / 型・関数 / テスト」の対応表を持つ
- 各アプリは、最小起動状態から 1 機能ずつ追加し、機能追加のたびに Mermaid の処理・データフロー図、コード説明、正常系・異常系テスト、実行結果を更新する。完成コードを一括提示して機能説明を後付けする構成は禁止する
- 各アプリの末尾に、入力項目の追加、永続化方式の変更、新しい API / ジョブの追加、監視項目の追加など、学習者がサンプルを発展させる拡張演習を最低 2 件含める
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 9. 実用処理（システム統合）

- FFI（C ライブラリ連携）
- 短周期タイマー（Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS / Ubuntu 24.04 LTS ARM64 対応の高精度ティッカー 3 種類。要求仕様: 付録 A、決定事項: `<出力ディレクトリ>/master-spec.md` の「付録 A 短周期タイマー決定事項」）
- 選択言語のシステム統合における固有機能（標準ライブラリ非依存ビルド、メモリレイアウト制御、ビルド制約、ランタイム制御など、OS / ハードウェア寄りの機能）
  - **対応中の言語での例**:
    - Rust: `no_std` / `no_alloc` ／メモリレイアウト（`repr(C)`、`repr(packed)`）
    - Go: build tag / ビルド制約 / `runtime` パッケージ
- セマンティックバージョニング処理
- メモリマップトファイル
- スレッドプール / ワーカプール（タスク並列実行のためのワーカ管理）
  - **対応中の言語での例**:
    - Rust: スレッドプール（`rayon`、`tokio::task` 等。採用は `<出力ディレクトリ>/master-spec.md` で決定）
    - Go: goroutine ベースのワーカプール（チャネル + 固定 goroutine 数）
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 10. 実践 CLI

- 環境構築（WSL2 ビルド、Windows VSCode デバッグ、Ubuntu 24.04 LTS x86_64 のネイティブ実行、Raspberry Pi ARM64 への SSH 配備・実行、macOS デバッグ手順）
- 基本構成（ログ / CLI / TOML / 非同期）
- CLI 入出力設計（標準出力 / 標準エラー、終了コード、JSON 出力、quiet / verbose）
- 引数・サブコマンド・設定ファイル・環境変数の優先順位
- シェル補完、ヘルプ、man 相当ドキュメント
- 長時間処理（進捗表示、キャンセル、シグナル、タイムアウト）
- CLI アプリ 12 種:
  1. tree 表示
  2. スクレイピング
  3. CSV → JSON 変換
  4. ログ解析 CLI
  5. 設定ファイル検証
  6. HTTP ヘルスチェック
  7. ファイル監視
  8. 簡易ジョブキュー
  9. ディレクトリ容量集計
  10. REST API クライアント
  11. テキスト差分
  12. Mailbox（メッセージ受信箱）/ Concurrent Queue デモ
- 上記 12 種をすべて個別の詳細学習ドキュメントと実行可能プロジェクトとして生成する。各ドキュメントには、利用シナリオ、要件、全機能一覧、入出力仕様、コマンド例、設定ファイル例、終了コード、ログ、エラー処理、テストケース、期待出力、トラブルシューティング、構築・テスト・配布手順を含める
- 各 CLI アプリに機能 ID 付きの「機能 / コマンド・オプション / 入力データ / 出力 / 担当ファイル / 型・関数 / テスト」対応表を設け、初心者が機能とコードのつながりを追えるようにする
- 各 CLI アプリは「起動する最小 CLI → 基本入出力 → 中核機能 → 設定・環境変数 → エラー処理 → ログ → テスト → 配布」のように 1 機能ずつ段階実装する。各段階で Mermaid の処理フロー、入力するコード、コードの読み方、実行コマンド、期待結果を示す
- 各 CLI アプリの末尾に、新しいサブコマンド、出力形式、入力元、フィルタ、エラー処理などを追加する拡張演習を最低 2 件含め、変更対象と確認方法を示す
- 単体・結合・システムテスト
- ゴールデンファイルテスト、スナップショットテスト、CLI 実行テスト
- CLI ビルド・配布
  - パッケージマネージャ経由配布（**自作 CLI を配布する側**：第 2 章「パッケージマネージャ経由のツール / バイナリのインストール」の対となる項目。利用者が `cargo install` / `go install` でインストールできる状態にする）
    - レジストリ公開のためのパッケージ構造とメタデータ（パッケージ名、バージョン、ライセンス、説明、公開可能性の設定）
    - Git URL 直結配布のためのリポジトリ構造（実行バイナリのエントリポイント、`main` / `bin` 宣言、リポジトリ URL の整合）
    - セマンティックバージョニング規約と Git タグ付け（タグ命名、リリース手順、過去バージョンの取得可能性）
    - **対応中の言語での例**:
      - Rust: `Cargo.toml` の `[package]`（`name` / `version` / `description` / `license` / `repository`）、`src/main.rs` または `[[bin]]` セクションによるバイナリ定義、`cargo publish` での crates.io 公開、Git タグ `vX.Y.Z` 付与
      - Go: `module` 宣言（リポジトリ URL と一致）、`main` パッケージのエントリポイント（典型は `cmd/<tool>/main.go`）、Git タグ `vX.Y.Z`（semver 必須）、公開リポジトリでの module proxy 経由取得
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 11. 実践 GUI

- GUI スタック（ホスト言語側の Web View 連携バックエンド + フロントエンドフレームワーク）
  - **対応中の言語での例**:
    - Rust: Tauri 2.0
    - Go: Wails
  - フロントエンドフレームワーク: React 19 / Vue 3 / Svelte 5（3 フレームワーク並列展開、全言語共通）
  - Node.js、パッケージマネージャ、ロックファイル、ビルドツール、単体 / E2E テストランナーの具体版と組み合わせは `<出力ディレクトリ>/master-spec.md` で決定する
  - 他言語の GUI スタックは `<出力ディレクトリ>/master-spec.md` で決定
- UI レイアウト基礎（4 種 × 3 フレームワーク）
- 基本コントロール（7 カテゴリ × 3 フレームワーク）
- MVVM / MVC（状態管理、データバインド・イベントバインド）
- ホスト言語とフロントエンド間の IPC / コマンド呼び出し / イベント通知
- 非同期処理と UI 状態（ローディング、キャンセル、エラー表示、再試行）
- フォーム設計と検証
- テーブル・フィルタ・ソート・ページング
- データベース連携
- 外部 UI コントロール
- アクセシビリティ、キーボード操作、テーマ、国際化
- GUI アプリ 6 種（× 3 フレームワーク）:
  1. シンプル GUI アプリ（簡易タスク管理）
  2. Single Window 業務アプリ（設定ファイル検証・変換）
  3. マルチウィンドウ業務アプリ（ログビューア）
  4. スマホアプリ的 UI（現場ダッシュボード）
  5. リッチ Web アプリ的 UI（社内ツール管理コンソール）
  6. VSCode 的 UI（データ解析・編集ワークベンチ）
- 上記 6 種 × React 19 / Vue 3 / Svelte 5 の **18 組み合わせすべて**について、個別の詳細学習ドキュメント、実行可能プロジェクト、構築手順、テスト手順、ビルド・配布手順を生成する
- 各 GUI アプリは、画面仕様、画面遷移、状態管理、IPC / コマンド呼び出し、入力検証、エラー表示、アクセシビリティ、E2E テスト、スクリーンショット確認、OS 別ビルド手順、期待される画面状態に加え、全機能を機能 ID 付きで列挙する
- 各 GUI アプリに「機能 / ユーザ操作 / 画面・コンポーネント / 状態 / IPC・バックエンド処理 / 担当ファイル・関数 / テスト」の対応表を設ける。画面遷移、状態遷移、IPC シーケンスは Mermaid で示し、図と実コードを対応付ける
- 各 GUI アプリは「空のウィンドウ → レイアウト → 1 つ目の操作 → 状態管理 → 永続化またはバックエンド連携 → 入力検証・エラー表示 → テスト → 配布」のように機能単位で段階実装し、各段階で起動可能な状態と期待画面を確認する
- 各 GUI アプリの末尾に、新しい画面、入力項目、フィルタ、データソース、ショートカット、テーマなどを追加する拡張演習を最低 2 件含め、設計変更からテストまで案内する
- 単体・結合・システムテスト
- GUI の単体・結合・E2E テスト（画面操作、状態検証、スクリーンショット比較）
- Raspberry Pi OS 64-bit / Ubuntu 24.04 LTS 64-bit で必要な ARM64 GUI・WebView・X11 依存関係を説明し、各 GUI アプリを MobaXterm の X11 フォワーディングで単体起動して目視確認する。デスクトップ全体の遠隔操作は扱わない
- Raspberry Pi の CI では仮想ディスプレイ上で GUI の単体・結合・E2E テストとスクリーンショット確認を自動化する。採用する仮想ディスプレイ、ウィンドウマネージャ、WebView 依存関係は `<出力ディレクトリ>/master-spec.md` で決定する
- 選択言語の GUI スタックが Linux ARM64、X11 フォワーディング、仮想ディスプレイのいずれかをサポートしない場合は、代替スタックを推測採用したり Raspberry Pi 検証を黙って省略したりせず、`<出力ディレクトリ>/master-spec.md` の未決定事項としてユーザ確認を行う
- ビルド・配布（署名、インストーラ、更新、OS 別成果物）
- **【テンプレート適用】**: 「ハンズオン作成方針」参照

### 12. CI/CD

- GitHub Actions（章ごとワークフロー分割）
- ワークフロー設計（main / PR / リリース / 定期実行の分離、トリガー、並列実行とジョブ依存）
- GitHub ホステッドランナーと Raspberry Pi セルフホステッドランナーの責務分離
  - Windows x86_64、Ubuntu 24.04 LTS x86_64、macOS arm64 のビルド・テストと ARM64 クロスビルドは GitHub ホステッドランナーを使用する
  - Raspberry Pi OS 64-bit ARM64 と Ubuntu 24.04 LTS 64-bit ARM64 のネイティブビルド・テストは、Raspberry Pi 4 以降の実機セルフホステッドランナーを基本とする
  - 実機は Raspberry Pi 4 以降のいずれか 1 台とし、ストレージまたは OS を切り替えて 2 OS を順番に検証する。2 OS のランナーが同時にオンラインであることを前提にせず、対象 OS の起動、実 OS / アーキテクチャとランナーラベルの事前照合、手動または承認付きジョブ実行、結果保存、停止、OS 切替を段階的に説明する
  - リリース候補のコミット SHA、ツールチェーン、依存ロック、テスト条件を固定し、2 OS の結果が同じ入力に対するものかを集約ジョブで検証する。オフライン側ランナーをプラットフォーム側のキュー上限まで待たせる設計を避け、対象 OS を起動してから明示的にジョブを開始する
- Raspberry Pi セルフホステッドランナーの構築と安全な運用
  - 専用の非管理者サービスアカウント、最小権限、ランナー登録・削除、サービス化、自動更新を含む更新方針、再起動、ジョブ間の作業ディレクトリのクリーンアップ、ディスク容量・温度・電源・ネットワーク状態の確認をハンズオンに含める。セルフホステッドランナーはジョブごとにクリーンな環境へ自動初期化されない前提で残留物を検査する
  - GitHub Actions の既定 `self-hosted` / `linux` / `ARM64` ラベルに加え、OS ディストリビューションを識別するカスタムラベルを使う。ジョブ開始直後に `/etc/os-release` と実アーキテクチャを検証し、ラベルと一致しなければコード実行前に失敗させる。具体的なラベル、ランナー名、OS 切替手順は `<出力ディレクトリ>/master-spec.md` で決定する
  - セルフホステッドランナーは、原則として非公開リポジトリかつ信頼済みコミットだけに使用する。fork PR、未レビューの PR コード、`pull_request_target` と未信頼コードの組み合わせを実行しない。`workflow_dispatch` または保護ブランチ上の承認済みコミットを起点とし、`GITHUB_TOKEN` を既定で読み取り専用、シークレットを最小限にする。SSH 鍵・ランナー登録トークンの保護、Action のコミット SHA 固定、ログへの機密情報混入防止も扱う
- ARM64 クロスビルドと実機検証
  - ホスト、ターゲット、ターゲットトリプルまたは `GOOS` / `GOARCH`、リンカ、OS 別 sysroot、glibc 互換性、CGO / FFI / ネイティブ依存関係を説明し、再現可能なクロスビルドを実装する。同じ `aarch64-unknown-linux-gnu` / `linux-arm64` であっても、Raspberry Pi OS と Ubuntu の成果物互換性を検証せずに仮定しない
  - 成果物名またはメタデータへ OS・アーキテクチャ・libc・バージョンを含め、チェックサムを生成して取り違えを防止する
  - GitHub ホステッドランナーで作った成果物をワークフローアーティファクトとして保存し、対象 OS で起動した Raspberry Pi セルフホステッドランナーが同一コミットの成果物を取得する。チェックサム、アーキテクチャ、動的依存関係を確認してから起動・テストし、クロスビルド成功だけを Raspberry Pi 対応の合格条件にしない
  - SSH / SCP / SFTP による手動配備は MobaXterm からの学習手順として別に扱う。GitHub ホステッドランナーから家庭内・社内ネットワークの Raspberry Pi へ外部向け SSH 接続を開放する構成を既定にしない
- Raspberry Pi GUI の CI 検証
  - 仮想ディスプレイ上で単体・結合・E2E テストとスクリーンショット確認を自動化する
  - MobaXterm の SSH / X11 フォワーディングによる単体 GUI アプリケーションの目視確認手順と結果記録を用意する。デスクトップ全体の遠隔操作は扱わない
- Raspberry Pi 追加分は、次の **4 項目を独立したハンズオン**としてすべて生成する。各項目は docs と project / workflow を対にし、完成 YAML・設定・コマンド・期待結果・失敗時の復旧を省略しない。これら以外の一般 CI/CD 項目は別件として台帳化する
  1. セルフホステッドランナーの構築・登録・安全な運用・OS 切替
  2. Raspberry Pi OS / Ubuntu ARM64 のネイティブビルド・テストと 2 OS の結果集約
  3. ARM64 クロスビルド、成果物保存、セルフホステッドランナーでの取得、依存関係確認、起動テスト、MobaXterm からの手動 SSH 配備
  4. 仮想ディスプレイによる GUI 自動テストと MobaXterm X11 フォワーディングによる目視確認
- 依存関係キャッシュ（パッケージ・ビルド成果物のキャッシュ戦略）
  - **対応中の言語での例**:
    - Rust: `Cargo` レジストリ／ビルドキャッシュ（`actions/cache` または `Swatinem/rust-cache`）
    - Go: `go mod` ダウンロードキャッシュ、`go build` キャッシュ
- マトリクスビルド（OS / ターゲット / バージョン軸の組み合わせ展開）
- 単体・結合・E2E テスト連携と結果レポート（テスト結果の集約、JUnit 形式での出力、PR コメントへの自動反映）
- カバレッジ計測とレポート
  - **対応中の言語での例**:
    - Rust: `cargo-llvm-cov`、`grcov` 等（採用は `<出力ディレクトリ>/master-spec.md` で決定）
    - Go: `go test -coverprofile`、`gcov2lcov` 等
- アーティファクト管理（ビルド成果物の保存、リテンション、ジョブ間共有）
- リリース自動化（バージョニング、changelog 生成、リリースノート、タグ生成、配布物アップロード）
- セキュリティスキャン（依存パッケージ脆弱性、シークレット検出、SAST、SBOM 生成）
  - **対応中の言語での例**:
    - Rust: `cargo audit`、`cargo deny`
    - Go: `govulncheck`、`gosec`
- Linter / Formatter / 静的解析の自動実行
- シークレット管理（GitHub Secrets、環境別設定、漏えい防止）
- 対象プラットフォーム / アーキテクチャ:
  - Windows x86_64
  - Ubuntu 24.04 LTS x86_64 glibc
  - Raspberry Pi OS 64-bit ARM64 glibc（Raspberry Pi 4 以降の実機）
  - Ubuntu 24.04 LTS ARM64 glibc（Raspberry Pi 4 以降の実機）
  - 補助サポート: macOS arm64 の CI ビルド、Linux x86_64 musl 相当のビルド
  - **対応中の言語での例**:
    - Rust: 正式対象は `x86_64-pc-windows-msvc` / `x86_64-unknown-linux-gnu` / `aarch64-unknown-linux-gnu`。補助ビルドは `aarch64-apple-darwin` / `x86_64-unknown-linux-musl`
    - Go: 正式対象は Windows `amd64`、Linux `amd64`、Linux `arm64` に対応する `GOOS` / `GOARCH` / `CGO_ENABLED` の組み合わせ。採用する具体値とネイティブ依存の扱いは `<出力ディレクトリ>/master-spec.md` で決定する

## 参照

- **選択言語用の出力仕様**: `<出力ディレクトリ>/output-spec.md` — 最終出力される学習ドキュメント・サンプルコード・CI/CD ワークフローの構造仕様および章別必須コンテンツチェックリスト
- **選択言語用の決定事項**: `<出力ディレクトリ>/master-spec.md`
- **選択言語用の Learning MISRA 仕様**: `<出力ディレクトリ>/misra-spec.md` — 具体ルール本文、適用例、検出方法、緩和条件
- **選択言語用の詳細仕様**: `<出力ディレクトリ>/spec.md`
- プロンプト群:
  - パイプライン実行プロンプト: `prompts/pipeline/`
  - 学習ドキュメント / サンプルコード / CI/CD ワークフロー生成プロンプト: `prompts/generation/<target_language>/`（通常パイプラインの `04-run-language-pipeline.md` 実行時に派生生成）

共通の出力仕様、共通の決定事項、共通の Learning MISRA ルールセット、言語別仕様書の共通雛形は参照・生成しない。

## `<出力ディレクトリ>/master-spec.md` で決定すべき項目一覧

本ファイル内で「`master-spec.md` で決定」「採用は `master-spec.md` で確定」と記載された項目を網羅する。`prompts/pipeline/02-derive-master-spec.md` は本表をチェックリストとして使用し、選択言語用の `<出力ディレクトリ>/master-spec.md` へ漏れなく反映する。非選択言語の決定事項は同ファイルへ含めない。

本表の各行は、決定対象の記述から選択言語への適用可否を判定する。特定の別言語だけを対象とする行は選択言語の `master-spec.md` へ転記しない。「各言語」「他言語の同等ツール」と記載された行は、選択言語に必要な同等決定だけを具体化する。派生完了報告では適用行と対象外行を区別して示すが、対象外言語の候補や決定を選択言語用ファイルへ保存しない。

| 章 | 項目 | 決定対象 |
|---|---|---|
| 全章 | ターゲットプラットフォーム基準 | Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi 4 以降 ARM64 の固定対象を確認し、Raspberry Pi OS 64-bit の対応リリースと最低版、Ubuntu 24.04 LTS の更新方針、CPU / libc / 動的依存関係の互換範囲を決定する。正式ターゲットに技術的な非対応がある場合は、ユーザ承認済みの理由・代替案・影響範囲・再検討条件を例外として記録する。Windows 70% / Linux 10% / Raspberry Pi 20% の配分自体は上流確定値として変更しない |
| 第 1 章 | 言語別ツールチェーン | 各 `target_language` のツールチェーン管理ファイル形式と版（C# / Python / Node.js は新規定義要） |
| 第 1 章 | バージョンマネージャ | Go の `asdf`（多言語一元）/ `g` / `gvm` / `goenv` のいずれを採用するか（`asdf` 非対応環境の補完を含む）。他言語の同等ツール |
| 第 1 章 | デバッガ拡張 | Rust: `CodeLLDB` または `rust-analyzer` 経由。Go: `Delve`。他言語の VSCode デバッガ拡張 |
| 第 1 章 | Raspberry Pi リモート開発 | MobaXterm の前提版・SSH 鍵方式・ホスト鍵確認・ファイル転送方法、Raspberry Pi 側 SSH / X11 設定、X 認証と必要パッケージ、単体 GUI アプリ確認コマンド、接続障害の診断方法 |
| 第 2 章 | パッケージマネージャ詳細 | 各言語の依存解決ルール、ロックファイル運用、レジストリ／プロキシ設定、`replace` 相当機構の使用ポリシー |
| 第 2 章 | パッケージマネージャ経由ツール配布 | 利用者向けインストール手順例、配布者向けリリース手順例の具体コマンド |
| 第 2 / 12 章 | ARM64 クロスビルド | クロスビルドホスト、ターゲットトリプルまたは `GOOS` / `GOARCH`、リンカ、Raspberry Pi OS / Ubuntu 別 sysroot と glibc 互換方針、CGO / FFI、ネイティブ依存、成果物命名・チェックサム、ワークフローアーティファクト受け渡し、MobaXterm からの手動 SSH 配備、実機での依存関係・起動確認方法 |
| 第 3 章 | ドキュメントコメント形式 | C# / Python / Node.js のドキュメントコメント形式 |
| 第 3 章 | 不在値表現の言語別採用基準 | Go の不在表現を `(T, bool)` と `(T, error)` のどちらにするかの節別基準 |
| 第 4 章 | コアテンプレートライブラリ群 | ロガー / 引数解析 / DI / 単体テストライブラリの言語別採用 |
| 第 5 章 | 非同期ランタイム | Rust の非同期ランタイム（`tokio` / `async-std` 等）の採用 |
| 第 5 章 | エラー処理応用ライブラリ | Rust の `thiserror` / `anyhow` / `eyre` 等の採用、retry / backoff クレートの採用 |
| 第 6 章 | C 比較実装ツールチェーン | Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS ARM64、Ubuntu 24.04 LTS ARM64 で使う C コンパイラと版、C 言語標準、警告をエラー化するフラグ、デバッガ、サニタイザまたは同等検査、ビルド・テストコマンド |
| 第 7 章 | 非同期 I/O プリミティブ | 各言語の非同期 I/O ランタイム採用、`epoll` / `kqueue` / `IOCP` 抽象の使用ライブラリ |
| 第 8 章 | シリアライゼーションライブラリ | テキスト形式（JSON / TOML / YAML）およびバイナリ形式（bincode / postcard / gob 等）の採用 |
| 第 8 章 | HTTP / Web API / DB ライブラリ | HTTP クライアント / サーバ、ルータ、ミドルウェア、入力検証、DB ドライバ、マイグレーション、コネクションプール、認証・認可方式の採用 |
| 第 8 章 | データ処理パイプライン | バッチ / ストリーム / キュー処理、retry / backoff、冪等性、デッドレター相当、メトリクスの採用 |
| 第 8 章 | Raspberry Pi ヘッドレス運用 | SSH 配備方法、サービス管理、専用ユーザと権限、環境変数、ログ、正常終了、再起動後の復旧、ディスク・メモリ制約への対応 |
| 第 9 章 | スレッドプール／ワーカプール | Rust の `rayon` / `tokio::task` 等の採用、Go のワーカプール基本パターン |
| 第 9 章 | 付録 A 短周期タイマー決定事項 | 各言語の OS API ラッパー、Go の CGO 使用判断、x86_64 / ARM64 別 spin ヒント、優先度設定 API、`spin_budget` 既定値、Windows・Ubuntu 24.04 LTS x86_64・Raspberry Pi OS / Ubuntu ARM64 別の対応版数下限と精度しきい値、Raspberry Pi の電源・温度・スロットリング・CPU 負荷の記録方法 |
| 全章 | Mermaid 検証 | Mermaid の構文・レンダリング検証ツール、バージョン、実行コマンド、CI での検証方法 |
| 第 11 章 | GUI スタック | 選択言語の Web View 連携バックエンドとバージョン（Rust: Tauri 2.0、Go: Wails、C# / Python / Node.js は追加決定。フロントエンドは React 19 / Vue 3 / Svelte 5 で固定） |
| 第 11 章 | フロントエンドツールチェーン | Node.js の版、パッケージマネージャと版、ロックファイル、ビルドツール、単体 / E2E テストランナー、ブラウザまたは WebView の取得・固定方針、Windows / Linux ARM64 での依存導入方法 |
| 第 11 / 12 章 | Raspberry Pi GUI 検証 | Linux ARM64 の GUI / WebView / X11 依存関係、X11 フォワーディング設定、CI 用仮想ディスプレイと必要なウィンドウマネージャ、E2E・スクリーンショット比較、目視確認記録の方法 |
| 第 12 章 | カバレッジツール | Rust の `cargo-llvm-cov` / `grcov` 等、Go の `gcov2lcov` 等の採用 |
| 第 12 章 | GitHub Actions 基盤 | GitHub ホステッドランナーイメージ、使用する Action とコミット SHA、`permissions`、依存キャッシュキー、アーティファクト名・保持期間・チェックサム・来歴、同一コミット成果物の受け渡し方法 |
| 第 12 章 | プラットフォーム軸の具体組み合わせ | Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS ARM64、Ubuntu 24.04 LTS ARM64 のネイティブ / クロス検証組み合わせ、Go の `GOOS` / `GOARCH` / `CGO_ENABLED`、Rust のターゲットトリプル、macOS / musl 補助ビルドの扱い |
| 第 12 章 | Raspberry Pi セルフホステッドランナー | リポジトリ公開範囲と信頼境界、1 台で 2 OS を切り替える登録・停止・再登録または再接続手順、ランナー名、OS 識別カスタムラベルと実環境の事前照合、専用サービスアカウント、最小権限、`workflow_dispatch` / 保護ブランチによるトリガーと承認、fork PR・未信頼コードの遮断、`pull_request_target` 制限、シークレット、Action の SHA 固定、自動更新を含む更新方針、ジョブ間クリーンアップ、監視、オフライン時の滞留回避、同一コミットに対する 2 OS の結果をリリース判定へ集約する方法 |

各項目を `<出力ディレクトリ>/master-spec.md` に反映する際は、「決定日 / 決定内容 / 採用理由 / 不採用候補 / 影響範囲 / 再検討条件」（`prompts/pipeline/02-derive-master-spec.md` の決定記録フィールド）を付与する。本表は本ファイル更新時に併せて更新し、漏れの検出は `prompts/pipeline/90-audit.md` で行う。

## MISRA ルール ID 体系

本ファイル付録 A は `LM-CORE-SEC-004` 等の具体ルール ID を参照する。これらの ID 命名規約とカテゴリ分類を本セクションで定める。具体ルールの列挙とルール本文は、選択言語用の `<出力ディレクトリ>/misra-spec.md` を単一ソースとし、本ファイルからは参照のみとする。

- **ID 形式**: `LM-CORE-{カテゴリ}-{3 桁連番}`（例: `LM-CORE-SEC-004`）
  - `LM`: Learning MISRA の固定接頭辞
  - `CORE`: コアルールセット識別子。将来サブセット（言語別 / 領域別の拡張ルール）が追加された場合は別識別子で分岐
  - `{カテゴリ}`: 下表のいずれか 3 文字大文字
  - `{3 桁連番}`: カテゴリ内の連番（`001` 始まり、欠番可・廃止 ID は再利用しない）
- **カテゴリ定義**:

| カテゴリ | 略号 | 範囲 |
|---|---|---|
| セキュリティ | `SEC` | 脆弱性発生源となるコードパターンの禁止・隔離（`unsafe` / FFI 境界、入力境界の検証、機密情報の取り扱い等） |
| 防御的プログラミング | `DEF` | バグ予防・例外耐性・並行安全性・リソース管理（`panic!` / `unwrap` 濫用禁止、リソース解放、キャンセル伝播、可観測性等） |
| 保守性 | `MNT` | コード可読性・予測可能性・OS 分岐の隔離・ドキュメント・スタイル統一 |

- ルール本体（適用例、検出方法、緩和条件、関連ルール）の記述は `<出力ディレクトリ>/misra-spec.md` を**単一ソース**とする
- 付録 A など本ファイル内で ID を参照する場合は ID のみを記述し、ルール内容の再掲は行わない。ただし、本ファイルが参照する `LM-CORE-SEC-004`、`LM-CORE-DEF-002`、`LM-CORE-DEF-003`、`LM-CORE-DEF-005`、`LM-CORE-DEF-006`、`LM-CORE-MNT-002` は上流と下流の**必須インタフェース**とし、選択言語用 `misra-spec.md` に同じ ID の適用可能なルール本文が存在しなければ派生を不合格にする
- 本ファイルから参照される ID の意味変更、再割り当て、廃止は、先に本ファイルと参照箇所を更新してから言語別 `misra-spec.md` へ派生する。選択言語だけで必要な追加ルールは、命名規約に従って言語別 `misra-spec.md` で決定してよいが、別言語のルールを混在させない

---

## 付録ポリシー

本ファイルの付録は、章本文の項目のうち**言語間で観測可能な挙動が等価であることを実装契約として保証する必要がある**項目に限定して詳細化する。下記いずれかを満たす項目が付録化の候補となる。

- **観測可能等価性が必要**: 同じ入力に対する精度・挙動（ヒストグラム、遅延分布、タイミング、シグナル順序など）を、言語間で観測上一致させる必要がある（精度比較・性能比較の学習目標を含む）
- **OS API の細部が学習目標**: `unsafe` / FFI / 低レベルシステムコールの具体名・引数・戻り値・エラー条件を学習者が直接扱う必要があり、本文章の項目記述では具体性が不足する
- **言語実装が大きく異なる**: 同じ要求を実現するために言語ごとに異なる構造（例: ファイル分割の `cfg` / build tag、CGO 経由 vs 純 Rust）が必要で、横断的な実装契約がないと不整合を起こしやすい

付録化の判断と既存付録の改定は、`languages-curriculum.md` 更新時の章本文と同じレビュー手順を経る。付録追加時は本セクションに「付録 X が追加された理由（上記いずれを満たすか）」を 1 行追記する。

現在の付録一覧:

| 付録 | 対象章 | 対象項目 | 付録化理由 |
|---|---|---|---|
| A | 第 9 章「実用処理（システム統合）」 | 短周期タイマー | 観測可能等価性が必要（Rust / Go で同じ測定方法・同じヒストグラム形式・同じ catch-up 挙動を達成する）／OS・アーキテクチャ API 細部（Windows `winmm` / Linux `timerfd` / x86_64・ARM64 spin ヒント）が学習目標／言語実装が大きく異なる |

将来 C# / Python / Node.js を `対応中` に昇格させる際は、付録 A §A.7「各言語実装の同等性契約」を本ポリシーの正準として参照し、同じ精度測定演習を追加実装で実施する。

## 付録 A: 短周期タイマー要求仕様（章 9 重点項目）

> 本付録は章 9「実用処理（システム統合）」の**短周期タイマー**に関する**要求 / 動機**を定める。
> 採用クレート・関数名・OS バージョン下限などの**決定事項**は `<出力ディレクトリ>/master-spec.md` の「付録 A 短周期タイマー決定事項」を参照。

### A.1 背景と動機

工場制御 / 計測 / リアルタイム描画 / 音声処理など、**ms 単位以下の周期で繰り返し処理を発火させる**ニーズに応える。言語標準の Sleep API（Rust: `std::thread::sleep` / Go: `time.Sleep`）だけでは以下の問題があり、短周期（1〜10ms）では要求精度を安定して満たせない。

1. **積算誤差**: Sleep を相対指定で繰り返すと、毎回の起床遅延が累積する
2. **粗い分解能**: Windows の既定タイマー分解能は 15.6ms と粗く、ms 精度の Sleep が要求通りに戻らない
3. **OS スケジューラの遅延**: 一般優先度のスレッドは他のプロセスに割り込まれる
4. **実機状態の影響**: Raspberry Pi では電源、温度、サーマルスロットリング、CPU ガバナー、同時負荷によって遅延分布が変化する

これらを「OS API の組み合わせ」と「実装パターン」で克服する 3 種類のティッカーを学ぶ。`unsafe` / FFI を**実用的な題材で初めて使う章**として、`LM-CORE-SEC-004`（unsafe は FFI 境界のみ）の典型適用例になる。

### A.2 提供する 3 種類のティッカー

| 種別 | 目標精度 | CPU 負荷 | 用途 |
|---|---|---|---|
| **Balanced** | ~0.5–0.8 ms | 中 | 推奨デフォルト。精度と難易度のバランス最良 |
| **HighPrecision** | ~0.5 ms 以下 | 高（最後の数百 µs を busy-loop） | 最高精度が必要な計測・音声処理 |
| **SimpleSleep** | ~1–2 ms | 低 | 簡易実装。学習用ベースライン |

上表の精度は設計時の目標値であり、全プラットフォームに同じ数値を無条件で保証する合格しきい値ではない。Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS ARM64、Ubuntu 24.04 LTS ARM64 ごとの基準値と許容差を `<出力ディレクトリ>/master-spec.md` で決定し、測定時の実機状態とともに記録する。

### A.3 設計要点（言語非依存の原則）

3 種すべてに共通する設計判断:

1. **絶対時刻管理**: 「次回発火時刻 = 起動時刻 + N × 周期」を絶対値で保持し、毎 tick の Sleep 引数を**残り時間**から計算する（積算誤差を防ぐ）
2. **catch-up ループ**: 起床が遅延して「次回発火時刻」を超過した場合、`while next <= now { next += interval }` でスキップ補正（過密 tick による山崩れを防ぐ）
3. **高精度カウンタ**: 起床判定には壁時計ではなく単調増加カウンタ（Rust: `std::time::Instant` / Go: `time.Now()` の monotonic 部 / Windows: `QueryPerformanceCounter` / Ubuntu・Raspberry Pi OS: `clock_gettime(CLOCK_MONOTONIC, ...)`）を使う
4. **RAII / `defer` による解放保証**: タイマー分解能の引き上げ操作（後述）は**必ず対称的に解放**。`LM-CORE-DEF-002`（リソース解放）の適用例
5. **キャンセル経路**: バックグラウンドスレッド / goroutine は `Drop` / `context.Context` でキャンセル可能にする（`LM-CORE-DEF-005` 適用）

### A.4 OS 別の実現手段（概念）

#### Windows

| 手段 | 役割 | 用途ティッカー |
|---|---|---|
| `winmm.dll: timeBeginPeriod(1)` / `timeEndPeriod(1)` | システムタイマー分解能を 15.6ms → 1ms に引き上げ。スコープ終了で必ず戻す | 全 3 種共通 |
| `kernel32.dll: CreateWaitableTimerExW` + `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION` フラグ | Windows の高精度ウェイタブルタイマー（Windows 10 1803+） | Balanced / HighPrecision |
| `SetWaitableTimerEx` + `WaitForSingleObject` | 相対 100ns 単位で one-shot 待機 | Balanced / HighPrecision |
| 短い busy-loop（spin-wait） | 最後の数百 µs を CPU で詰めて精度を稼ぐ | HighPrecision のみ |
| Sleep(1ms) + 高精度カウンタ判定 | timeBeginPeriod(1) 下で 1〜2ms 精度の最小実装 | SimpleSleep |

#### Linux（Ubuntu x86_64 / Raspberry Pi OS・Ubuntu ARM64）

| 手段 | 役割 | 用途ティッカー |
|---|---|---|
| `timerfd_create(CLOCK_MONOTONIC, ...)` + `read` ブロッキング | カーネルタイマーを fd 化、`read` で目覚める | Balanced / HighPrecision |
| `timerfd_settime` + `TFD_TIMER_ABSTIME` | 絶対時刻指定で one-shot / 周期発火 | Balanced / HighPrecision |
| アーキテクチャ別の CPU spin ヒント（x86_64: `PAUSE`、ARM64: `YIELD`）を使う短い busy-loop | 最後の精度詰め。`sched_yield()` はタイムスライスを譲って粒度が粗くなるため**不可** | HighPrecision のみ |
| `clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME, ...)` | 絶対時刻指定の sleep（積算誤差なし） | SimpleSleep |

備考: Ubuntu と Raspberry Pi OS は Linux の同じタイマー API を共有するため、Windows のような `timeBeginPeriod` 相当の事前準備は不要。ただし Raspberry Pi 実機では、測定前に電源状態、CPU 温度、スロットリング、CPU 負荷、周期中の skipped tick 数を記録する。リアルタイム保証が必要なら `SCHED_FIFO` 等を検討する余地はあるが、本カリキュラムでは正式実装対象外とする（権限要求が大きく学習用途に不向き）。

### A.5 適用される Learning MISRA ルール

| ルール | 適用箇所 |
|---|---|
| `LM-CORE-SEC-004` | Windows API / Linux syscall は `unsafe` / FFI 境界に隔離。安全なラッパー型で公開 |
| `LM-CORE-DEF-002` | `timeBeginPeriod` / `timerfd` / WaitableTimer のハンドルを RAII / `defer` で必ず解放 |
| `LM-CORE-DEF-003` | バックグラウンドスレッド / goroutine の共有可変状態を最小化、キャンセルは `AtomicBool` / `context.Context` |
| `LM-CORE-DEF-005` | タイマー停止 = `Drop` / `Stop()` のタイムアウト付き合流（最大 2 秒など） |
| `LM-CORE-DEF-006` | tick 遅延（実発火時刻 − 期待時刻）をヒストグラムでメトリクス化 |
| `LM-CORE-MNT-002` | OS 分岐は薄いアダプタ層（`adapter/timer_windows.rs` / `timer_linux.rs`）、アーキテクチャ固有の spin ヒントは x86_64 / ARM64 別の最小ヘルパーに閉じ込める |

### A.6 実装方針（言語非依存）

- **OS・アーキテクチャ切替**: Rust は `#[cfg(target_os = "windows")]` / `#[cfg(target_os = "linux")]` と `#[cfg(target_arch = "x86_64")]` / `#[cfg(target_arch = "aarch64")]`、Go は `//go:build windows` / `//go:build linux` および `amd64` / `arm64` の build constraint で分割する。ティッカー本体は OS 対称な 3 種 × 2 OS アダプタ = 6 論理実装を保ち、spin ヒントだけをアーキテクチャ別の小さなヘルパーへ分離する
- **共通インタフェース**: 3 種に共通する `Ticker` トレイト / インタフェース（`new(interval, on_tick)` / `start()` / `stop()`）を定義し、利用側は OS / 種別を意識せず使える
- **演習**: 1ms / 2ms / 5ms / 10ms の 4 周期 × 3 種類を、Windows x86_64、Ubuntu 24.04 LTS x86_64、Raspberry Pi OS 64-bit ARM64、Ubuntu 24.04 LTS ARM64 の 4 環境で測定する。Raspberry Pi の 2 OS は 1 台の実機を切り替えて順番に実行し、実発火時刻、ヒストグラム、p99、skipped tick、電源・温度・スロットリング・CPU 負荷を記録する
- **macOS 扱い**: 章 9 では正式実行対象外（CI ビルドのみ）。公開 API は macOS でもビルド可能にし、実行時は `UnsupportedPlatform` 相当のエラーを `Result` / `(T, error)` で返す。Windows / Linux 専用の低レベル実装は `cfg` / build tag で隔離し、macOS に `unimplemented!` / `panic("not supported on macOS")` を置かない

### A.7 各言語実装の同等性契約

§A.1〜§A.6 の設計を踏まえ、各言語実装が**同一プラットフォーム上で観測可能な挙動として等価**となるよう、以下の契約を必ず満たす。本付録の §A.1〜§A.6 は「概念と動機」、本節 §A.7 は「実装契約（チェックリスト）」の位置付け。プラットフォーム間では測定方法・出力形式・catch-up 契約を統一し、p99 などの数値しきい値は `<出力ディレクトリ>/master-spec.md` の環境別基準を使う。

#### A.7.1 公開 API

3 種ティッカー共通の `Ticker` 型を以下の意味論で定義する（具体的シグネチャは言語慣例に従う）：

- **コンストラクタ**: `new(interval, on_tick)` — HighPrecision のみ任意で `spin_budget` を受け取る（既定 200 µs）。OS リソース獲得失敗（`timeBeginPeriod` / `CreateWaitableTimerEx` / `timerfd_create` 等）は `Result<T, E>` / `(T, error)` で返す（`panic!` / `unwrap` 禁止）
- **入力検証**: コンストラクタは OS リソース獲得**前**に以下を検証し、違反時は即座にエラーを返す：
  - `interval > 0`（ゼロ／負はエラー）
  - HighPrecision の場合 `0 < spin_budget < interval` を満たす
  - `on_tick` コールバックの存在（言語の null 表現で「未設定」と区別できる場合に限る）
  - `on_tick` は `Result<(), E>` / `error` 相当を返す契約にし、通常の失敗をパニック／例外で表現しない
- **`start()`**: バックグラウンドスレッド／goroutine を起動。多重呼出しは言語慣例に従いエラー or 無視
- **`stop()` または `Drop` / `defer Close()`**: 停止要求 → ワーカ合流（**最大 2 秒** Join タイムアウト）。`stop()` 後の再開は不要（破棄して再構築）

#### A.7.2 ティッカー別パラメータ

| 種別 | スレッド優先度 | 終端 spin | spin バジェット（既定） |
|---|---|---|---|
| **Balanced** | 高め（Windows: `THREAD_PRIORITY_HIGHEST` 相当 / Ubuntu・Raspberry Pi OS: 通常優先度、権限がある場合のみ nice 値を調整） | なし | — |
| **HighPrecision** | 高め（同上） | あり | **200 µs**（コンストラクタで上書き可） |
| **SimpleSleep** | 一段上（Windows: `THREAD_PRIORITY_ABOVE_NORMAL` 相当 / Ubuntu・Raspberry Pi OS: 通常） | なし | — |

優先度設定の試行戦略:

- **Windows**: `SetThreadPriority(THREAD_PRIORITY_HIGHEST / THREAD_PRIORITY_ABOVE_NORMAL)` を直接呼ぶ（通常権限で成功）
- **Ubuntu / Raspberry Pi OS**: `SCHED_FIFO` は正式実装対象外。通常優先度で動作させ、権限がある場合のみ `setpriority(PRIO_PROCESS, 0, nice 値)` 等の best-effort 調整を試行する。失敗時は警告ログを残して既定優先度で続行（**停止理由にしない**）

#### A.7.3 メインループ構造（疑似コード）

```text
next = now() + interval               // 初回発火は start 時刻 + 1 interval
loop {
    if cancelled() { break }          // ① ループ先頭
    if next > now() {
        sleep_until(next)             // 種別別の待機（§A.7.4 参照）
    }
    if cancelled() { break }          // ② sleep 復帰直後
    result = on_tick()                // 通常失敗は Result / error で返す
    if result is error {
        record_error(result)
        break
    }
    next = next + interval
    while next <= now() {
        next = next + interval        // catch-up は on_tick の後ろで補正
    }
}
```

- キャンセル確認は最低 **2 箇所**（ループ先頭、sleep 復帰直後）。`stop()` 要求から最大 2 秒（Join タイムアウト）で停止することを担保
- `on_tick` の通常失敗は `Result` / `error` で返し、ワーカ側で記録して停止する。パニック／例外は不変条件違反として扱い、正常系のエラー伝播に使わない
- catch-up ループは `on_tick` 呼出しの**後ろ**に置く（前に置くと最初の遅延が `on_tick` の発火回数に影響する）
- **オーバラン観測**: `now() - 期待発火時刻` を tick 毎に観測し、`interval` を超える遅延が発生した場合は警告レベルでログ／メトリクス化する（`LM-CORE-DEF-006` で言及する tick 遅延ヒストグラムの根拠）。catch-up でスキップした tick 数も計上対象とする

#### A.7.4 `sleep_until` の種別別実装

| 種別 | Windows x86_64 | Ubuntu x86_64 | Raspberry Pi OS / Ubuntu ARM64 |
|---|---|---|---|
| **Balanced** | `WaitableTimerEx`（HIGH_RESOLUTION）に `next - now()` を相対設定 → `WaitForSingleObject` でブロック | `timerfd_settime(TFD_TIMER_ABSTIME, next)` → `read(2)` でブロック | Ubuntu x86_64 と同じ Linux API を使用 |
| **HighPrecision** | `WaitableTimerEx` で `next - now() - spin_budget` まで待機 → x86_64 `PAUSE` 相当で `next` まで詰める | `timerfd_settime` で `next - spin_budget` まで待機 → x86_64 `PAUSE` 相当で詰める | `timerfd_settime` で `next - spin_budget` まで待機 → ARM64 `YIELD` 相当で詰める |
| **SimpleSleep** | `Sleep(1ms)` を `now() < next` の間繰り返す（`timeBeginPeriod(1)` 下） | `clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME, next)` を 1 回呼出し | Ubuntu x86_64 と同じ Linux API を使用 |

#### A.7.5 Windows FFI の細部

- `timeBeginPeriod(1)` / `timeEndPeriod(1)` は対称呼出し（RAII / `defer` で必ず）
- `CreateWaitableTimerExW` フラグ: `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION` (= `0x00000002`)
- `CreateWaitableTimerExW` の `dwDesiredAccess` 引数: `TIMER_ALL_ACCESS` (= `0x1F0003`)
- `SetWaitableTimerEx` の `lpDueTime`: **負値**で「相対時間（100 ns 単位）」を表す（正値は FILETIME 絶対時刻）。Rust では `i64`、Go では `int64` で符号付き整数として扱う
- `SetWaitableTimerEx` の `tolerableDelay`: `0`（許容遅延なし＝最大精度）
- `WaitForSingleObject` のタイムアウト: `INFINITE` (= `0xFFFFFFFF`)。戻り値が `WAIT_OBJECT_0` (= `0`) 以外（`WAIT_FAILED = 0xFFFFFFFF`、`WAIT_ABANDONED = 0x00000080` 等）は失敗扱いで `Result` / `(T, error)` に伝播
- 終端 spin: Rust は `std::hint::spin_loop()`。Windows x86_64 の Go は標準 API がないため、CPU `PAUSE` 相当のヒントを **Go アセンブリ**で発行する（CGO 経由 `_mm_pause` 等も選択肢。採用は `<出力ディレクトリ>/master-spec.md`。純 Go / `CGO_ENABLED=0` 方針では Go アセンブリを優先）。`runtime.Gosched()` はタイムスライスを譲ってしまうため**不可**

#### A.7.6 Linux x86_64 / ARM64 FFI の細部

- 高精度時刻: `clock_gettime(CLOCK_MONOTONIC, ...)`（言語ラッパー `Instant` / `time.Now()` の monotonic 部で代替可）
- `timerfd_create` のフラグ: `TFD_CLOEXEC` を付けて fork 後の継承を防ぐ
- `timerfd_settime` の `flags`: `TFD_TIMER_ABSTIME`、`new_value.it_interval` = 0（one-shot）、`new_value.it_value` に次回発火の絶対時刻（`struct itimerspec`）
- スレッド優先度設定: §A.7.2 の方針に従い、通常権限で継続可能な best-effort 調整に限定する。`setpriority(PRIO_PROCESS, 0, nice 値)` 等を試行し、失敗時は警告ログを残して既定優先度で継続する
- 終端 spin: Rust は `std::hint::spin_loop()`、Go は x86_64 で `PAUSE` 相当、ARM64 で `YIELD` 相当の CPU ヒントをアーキテクチャ別ヘルパーから発行する。**`sched_yield()` は不可**（タイムスライス放棄で粒度が粗くなる）
- SimpleSleep: `clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME, &timespec, NULL)` で `struct timespec` の絶対時刻指定 1 回呼出し（積算誤差なし）

#### A.7.7 C# 実装との比較（将来）

将来 `LearningCSharp2026/` に C# 実装が追加された際は、本節の契約を正準として参照する。Rust / Go 実装は本契約を満たした上で、§A.6 の精度測定演習を C# 実装と**同条件**で行い、ヒストグラム / p99 を比較する。有意な乖離があれば原因を分析し、章末ドキュメントに記録する。
